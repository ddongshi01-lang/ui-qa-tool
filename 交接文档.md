# 视觉走查助手交接文档

## 今日补充（2026-04-15，背景实色摘要行内联编辑，渐变仍未收口）

这轮主要在 `visual-qa.js` 里调整背景区的信息架构，把“实色”和“渐变”的编辑入口拆开了，但目前只把实色的主编辑路径前置到摘要行，渐变这块还没有完全改好，后续接手时要把它当成未收尾状态处理。

### 1. 已经推进的内容
- 实色背景现在可以直接在背景摘要行里编辑 `hex` 和透明度。
- 背景摘要行里保留了左侧预览、类型文案、透明度和删除按钮。
- 实色编辑不再强依赖“颜色属性”弹层作为主入口。
- 代码里已经把背景摘要行拆成了按模式分流的结构，实色和渐变不再共用同一条展示逻辑。

### 2. 目前还没改好的地方
- 背景色渐变的摘要行和弹层交互还没完全收好。
- 渐变的角度、两个色标、以及摘要行与弹层之间的切换，还需要继续整理。
- 当前这轮不能视为“渐变背景已完成重构”，它仍然是需要回头补完的重点。

### 3. 当前交接口径
- 后面继续做背景区时，优先保住实色摘要行的直接编辑能力。
- 渐变仍建议按“弹层编辑、摘要行只保留快捷调和删除”的方向补完。
- 这次只算完成了背景区拆分的前半段，不要把它当成最终版。

## 今日补充（2026-04-13，退出口径、扩展图标两态、数值拖动保护与光标反馈）

这轮改动没有重做选择/测量/记录的主状态机，重点是把插件退出口径、扩展图标状态同步，以及选择浮窗数值拖动时的页面保护做干净，避免真实使用时出现“按 Esc 直接退插件”或“拖值结束误选页面元素”这类明显断流的交互问题。

### 1. 插件退出口径已经改成“只认扩展图标点击”
- 旧的 `Esc = 退出插件` 已经移除。
- 当前实例只能通过再次点击浏览器扩展图标关闭。
- `destroy()` 现在只接受扩展图标点击触发，不再允许页面内 `keydown(Esc)` 直接销毁实例。
- `visual-qa.js` 顶部仍然保留“已存在实例则走销毁”的开关式生命周期，但触发源已经收敛到 `chrome.action.onClicked`。

### 2. Esc 的当前规则
- 最外层空闲态：
  - 按 `Esc` 不做任何事。
  - 绝对不会退出插件。
- 选择/测量态下如果存在 `measureA / measureB / guides / 高亮 / 临时测量渲染`：
  - `Esc` 只清当前测量选中和相关渲染。
  - 不会退出插件。
- 记录相关临时态：
  - 记录浮窗打开时，`Esc` 关闭浮窗。
  - 区域框选中时，`Esc` 取消框选。
  - 记录子菜单、预览、清空确认等临时层打开时，`Esc` 只关闭当前层。
  - 以上都不会触发 `destroy()`。
- 后续如果再新增 popup / menu / overlay 一类的临时 UI，也继续沿用同一口径：`Esc` 只负责关当前层，不负责退插件。

### 3. 扩展图标已经补成 tab 级 ON / OFF 两态
- `service_worker.js` 现在把当前 tab 的插件状态视为浏览器 action 的单一来源。
- 当前 tab 打开插件成功后：
  - action icon 切到 `ON`
  - badge 显示 `ON`
  - title 变成 `视觉走查助手（已开启）`
- 当前 tab 销毁插件后：
  - action icon 恢复 `OFF`
  - badge 清空
  - title 变成 `视觉走查助手（未开启）`
- 状态同步通过 `content-bridge.js` / `visual-qa.js` / `service_worker.js` 的消息桥完成。
- 每个 tab 独立维护图标状态；tab reload / url 变化时会回到 `OFF`。
- action 图标资源当前使用静态 PNG，不走 SVG 动态生成：
  - `icons/action-on-16.png`
  - `icons/action-on-32.png`
  - `icons/action-off-16.png`
  - `icons/action-off-32.png`

### 4. 选择浮窗数值拖动已经加了 document 级保护
- 拖值入口包括两类：
  - `Option/Alt + mousedown` 的输入框正文拖值
  - 输入框图标热区 `mousedown` 的左右拖值
- 进入 numeric scrubbing 后：
  - 页面 hover 命中暂停
  - 页面 click / mouseup 选中暂停
  - 选择模式里的 `measureB` 更新暂停
  - 不允许切换 `measureA`
- 拖值过程中允许鼠标移出编辑框。
- 即使在编辑框外松开，也只会结束本次拖值，不会选中鼠标落点的页面元素。
- 结束拖值时会先提交最终值，再设置一次性 suppress，吞掉紧跟着的 `mouseup/click`，随后恢复正常页面选择逻辑。
- 这层保护不是只靠输入框局部 `stopPropagation()`，而是在 document 主链路前统一短路：
  - `onMouseMove`
  - `onClick`
  - `fromPoint()`
  - `handleMeasureModeClick()`
  - 以及 `measureB` 的 hover 写入分支

### 5. 数值拖动的光标反馈也已经提前
- 图标热区 hover：
  - 现在直接显示 `ew-resize`
  - 不需要等真正开始拖值
- 输入框正文区域：
  - 普通状态保持文本光标
  - 按住 `Option/Alt` 时立即切成 `ew-resize`
  - 松开 `Option/Alt` 时立即恢复文本光标
- 真正进入拖值态后：
  - 无论鼠标是否还在编辑框内，都会持续保持 `ew-resize`
  - 直到 `mouseup/pointerup` 为止
- 当前实现已经把 cursor 判断拆成三层：
  - 图标热区 hover
  - 输入框正文 hover + `Option/Alt`
  - numeric scrubbing active

### 6. 这轮明确没动的部分
- 没有重写 `measureA / measureB` 主状态机。
- 没有改记录功能、抽屉、草稿恢复、HTML 导出。
- 没有改字段提交、重置、measureA 绑定的主链路。
- 当前改动目标只是在现有结构上把“退出源”“图标状态”“拖值保护”“拖值光标反馈”补齐。

## 今日补充（2026-04-12 ~ 2026-04-13，浮窗面板重做与图标资源接入）

这两天的更新还是集中在 `visual-qa.js` 的选择态浮窗，但方向已经从“继续抠交互细节”转成“把右侧面板的视觉骨架和资源链路一起收口”。同时，`service_worker.js` 和 `manifest.json` 也补了新一批浮窗图标资源，让面板里的布局、字体、外观控制都能直接吃到独立 SVG。

### 1. 选择态面板整体换成更像 Figma 的深色 atom 风格
- 面板背景、边框、阴影、圆角和间距都重新调过，整体更紧凑。
- 颜色体系收敛到更统一的蓝色高亮，减少了原来多色混用的感觉。
- `selected panel` 的标题逻辑也更明确了：能编辑文本时显示“文本”，否则显示“元素”。
- 面板内容从原来偏表单式的双列布局，改成了更偏卡片化的 atom 网格结构。

### 2. 布局、字体、外观区块都换成了图标驱动的控件
- 新增了一批浮窗专用 SVG 图标资源，放在 `assets/ui/fuchuang/`。
- 布局区现在直接用 `layout-width`、`layout-height`、`layout-padding-*` 这些图标来表达宽高和内边距。
- 字体区把字族、字重、字号、行高和字色都压成了更统一的行内控件。
- 外观区也改成了更一致的图标输入块，圆角展开后会继续显示四个角的独立值。

### 3. 浮窗资源链路已经接好
- `manifest.json` 里新增了 `assets/ui/fuchuang/*.svg` 的 web accessible resources。
- `service_worker.js` 新增了 `get-floating-icon-urls()`，把浮窗图标路径集中输出给前端。
- `visual-qa.js` 里增加了获取这批图标的桥接逻辑，避免浮窗先渲染、图标后到导致的空位闪动。

### 4. 交互上做了几处收口
- 选择态面板 hover 现在会更稳定地冻结页面 hover，避免编辑右侧面板时主页面高亮乱跳。
- 拖动浮窗时会先判断是否真的发生位移，只有真实拖动后才抑制下一次 click。
- 关闭和销毁时会额外清理 `change` 事件和注入的样式节点，减少残留状态。
- `visual-qa.js` 里也顺手把一些 overlay 样式抽成了更统一的 helper，后面继续调色会更省事。

### 5. 这轮没有动的大块
- 测量主逻辑、记录/导出链路、草稿恢复、drawer 分支没有改。
- 选择模式、测量模式、记录模式的整体分工也没有重写。
- 这两天的重点仍然是“选择态浮窗的视觉与资源收口”，不是重做底层能力。

## 今日补充（2026-04-10，选择浮窗编辑面板骨架强收口）

这轮改动仍然只收在 `visual-qa.js` 的选择态编辑浮窗，不碰测量主逻辑、记录/导出链路、草稿恢复和 drawer 分支。目标不是继续整理 helper，而是直接把右侧编辑面板的外壳、section 节奏、字段块和颜色行压到更接近 Figma 的骨架，让截图一眼能看出面板已经换了结构。

### 1. 面板外壳尺寸已经明显收窄
- `selected panel` 的宽度从原来的 `304-352px` 收到 `286-304px`。
- 面板圆角从 `12px` 压到 `10px`，头部和内容区留白也一起缩紧。
- 顶部不再保留旧弹窗式的松散头部感，改成更短的拖拽条结构。
- 头部标题字号从 `12px` 压到 `11px`，整体视觉更轻、更紧。

### 2. 字段布局已经改成统一块状骨架
- 不再沿用“上方小标签 + 下方输入框”的旧表单节奏。
- 宽 / 高、字族 / 字重、字号 / 行高这些双列项现在都进入统一的块状布局。
- 字段 label 变小，字段卡片之间的留白也被压缩，面板里会更像连续的设计稿栅格。
- 展开按钮已经收成右侧独立的小操作位，不再像普通按钮塞在字段流里。

### 3. section 节奏已经整体压紧
- section 标题字号从 `12px` 压到 `11px`。
- section 之间的间距从 `12px` 压到 `8px`。
- 字段内部间距也从 `8px` 压到 `6px` 左右，整体更像 Figma 里的紧凑属性面板。
- 文本态也已经被压进同一套更紧的 section 节奏里，不再只是逻辑分流。

### 4. 颜色行已经改成横向组合
- 颜色行已经从旧输入框拼接感改成横向组合结构。
- 左侧是色值块，右侧是透明度块。
- 两侧都采用统一的卡片尺寸、圆角和对齐方式。
- 这一行现在更接近设计稿里那种“一整行组合控件”的感觉。

### 5. 这轮仍然完全没动的部分
- `buildSelectedCapabilities`
- `applyStyle`
- `onPanelInput`
- `onPanelClick`
- `onPanelKeyDown`
- scrub 逻辑
- 拖拽定位
- 顶部工具栏
- 测量 / 记录 / 抽屉 / 导出
- `manifest` / 图标路径

### 6. 当前口径
- 这轮的重点是“强可见差异版结构收口”，不是继续做抽象 helper 整理。
- 后续如果再往下推，优先继续压外壳、字段块和 section 节奏，再进入像素级样式微调。

## 今日补充（2026-04-10，选择浮窗字段与透明度规则统一）

这次改动仍然只收在 `visual-qa.js` 的选择态编辑浮窗，不碰测量主逻辑、记录/导出链路、草稿恢复和 drawer 分支。当前口径已经统一到“选择浮窗只负责当前选中目标的字段编辑”，并把透明度相关交互整理成同一套百分比体验。

### 1. 透明度输入已经统一成百分比语义
- `字色透明度` 和 `整体透明度` 现在都按百分比显示和编辑。
- 两个字段都支持：
  - `ArrowUp / ArrowDown` 逐步调整
  - `Shift + ArrowUp / ArrowDown` 快速步进
  - `Alt + 拖拽 scrub` 快捷调整
- 输入框里可以直接选中或删除 `%`，但输入数字后会自动恢复 `%` 展示。
- 清空后会回退到当前计算值，不会保留空态。

### 2. 字色和整体透明度仍然职责分离
- `字色透明度` 只改 `color` 的 alpha，不碰元素整体 `opacity`。
- `整体透明度` 只改元素级 `opacity`，不反推或混用颜色 alpha。
- 颜色值、色块预览和透明度百分比是分开的三段，不再共用单一数值逻辑。

### 3. 选择浮窗的字段结构已经调整到当前形态
- 字色行不再显示标题“字色”，只保留控件本体。
- 圆角支持展开编辑四个角：
  - `左上`
  - `右上`
  - `右下`
  - `左下`
- 内边距收起时显示 `水平 / 垂直`，展开后只保留四边输入，避免重复展示。
- 外边距仍然保持按边显示和混合态处理，不改之前的写回语义。

### 4. 选择浮窗默认定位也已经更新
- 浮窗首次出现时默认停到浏览器右侧，距离边缘约 `90px`。
- 仍然保留拖动能力。
- 拖动后的 `panelX / panelY` 记忆逻辑没有改。

### 5. 记录浮窗的快捷键屏蔽口径已经收口
- 记录浮窗打开时，`V / C / O / R / M` 这组顶栏快捷键会整体失效。
- 这不是只在输入框 focus 时禁用，而是把 `pendingRecord` 视为轻量 modal，整个浮窗 open 期间都屏蔽顶栏模式切换和 drawer 开关。
- 浮窗关闭后，这组快捷键会自动恢复。
- 浮窗内部的备注输入、分类下拉、保存、取消和 `Esc` 关闭仍然可用，不受这条屏蔽影响。

### 6. 现状口径
- 选择模式与测量模式的分工没有变。
- `measureA / measureB`、single / pair / containment / enhancement 规则没有变。
- 记录元素、记录区域、记录抽屉、草稿恢复、HTML 导出没有变。
- 如果后面再继续调整选择浮窗，优先在这版交接口径上增量修改，不要回头恢复旧的数值输入体验。

## 今日补充（2026-04-09，顶部条信息架构重排）

这次改动只收在 V1.2 顶部条和状态切换层，不动记录浮窗、记录抽屉内容区、导出、草稿恢复和测量内部逻辑。核心目标是把原来“记录”下拉入口拆成两个顶层按钮，并把最右侧入口收成只显示数量的 drawer 开关。

### 1. 顶部结构已经改成固定顺序
- 顶部现在固定为：
  - `选择`
  - `测量`
  - `记录元素`
  - `记录区域`
  - 数字入口
- 原先顶部的“记录” split 按钮和下拉菜单逻辑已经移除。
- 顶部不再展示“记录列表”文字，最右侧只显示当前真实记录数量。

### 2. 顶层状态切换已经拆开
- `mode` 现在明确支持：
  - `select`
  - `record-element`
  - `record-region`
- `record-element` 和 `record-region` 已经变成两个独立的顶层可点击按钮，不再依赖记录下拉菜单去二次选择。
- 点击“记录元素”会直接进入元素记录态。
- 点击“记录区域”会直接进入区域框选态。
- 这两种状态仍然共享现有的记录创建、浮窗保存和抽屉落盘逻辑，没有重写记录数据链路。

### 3. 数字入口的职责已经独立出来
- 最右侧数字入口只负责 drawer open / close。
- drawer 打开时，数字入口显示 active 态。
- drawer 关闭时恢复普通态。
- 数字入口的数值绑定真实记录数量，不再写死占位。
- 数字入口不再改变当前 `mode`，和记录态是彼此独立的状态。

### 4. 保持不动的部分
- 记录浮窗仍然沿用原来的创建、输入、保存和取消逻辑。
- 记录抽屉内部列表结构没有改。
- 截图、草稿恢复、导出逻辑没有改。
- 测量模式内部的 single / pair 规则没有改。
- 选择模式原有链路没有改。

### 5. 代码落点
- 顶部条结构和按钮渲染主要在 `visual-qa.js` 的顶部条 DOM / sync 逻辑中。
- `select`、`measure`、`record-element`、`record-region`、`toggle-drawer` 的顶层动作已经重新收口。
- `recordMenu` 相关的旧下拉层虽然还留有部分兼容代码，但顶部不会再展示，也不会再作为主要交互入口。

### 6. 现状阅读口径
- 下面 2026-04-02 / 2026-04-03 的条目保留的是功能演进过程。
- 如果这些历史段落和当前 `2026-04-09` 口径冲突，以本节为准。
- 现在判断 V1.2 入口逻辑时，只认当前顶部固定结构，不再按旧的“记录 split + 菜单”理解。

## 今日补充（2026-04-09，晚，选择模式接入测量状态机）

这轮改动的目标不是重做测量模式，而是把当前已经稳定的测量展示能力平移到顶部“选择”模式里，同时只保留选择模式原本最有价值的一项能力：选中元素后出现编辑框。整个改动仍然只收在 `visual-qa.js`，没有动记录元素、记录区域、抽屉、草稿恢复、导出和 pair 算法本体。

### 1. 当前“选择”和“测量”的关系
- 顶部仍然保留两个模式：
  - `选择`
  - `测量`
- `测量` 模式保持原样，继续是纯测量工作流。
- `选择` 模式现在也接入了同一套测量状态机：
  - click 锁定 `measureA`
  - hover 有效目标写入 `measureB`
  - single / pair / containment / enhancement 的显示规则直接复用测量模式现有能力

### 2. 选择模式只额外保留“编辑框”
- 选择模式下，编辑框不再以旧的 `selectedA / frozenEl` 作为主绑定目标。
- 当前编辑框主目标改成：
  - 选择模式优先绑定 `measureA`
- 这意味着：
  - 点击 A 后会立即出现原有编辑框
  - hover B 进入 pair 时，编辑框仍然持续显示 A 的内容
  - 测量模式不会显示编辑框

### 3. 当前刻意没有做的事
- 没有大面积删除：
  - `selectedA`
  - `frozenEl`
  - `primaryMeasure`
  - 相关旧函数和旧状态
- 本轮只要求这些旧状态不再驱动选择模式主展示，优先通过停用 / 绕过 / 最小化保留来兼容。
- 没有改：
  - `resolveMeasureSingleState()` 的 structure / leaf 分类
  - persistent / hover-only 规则
  - pair / containment 既有算法

### 4. 当前对选择模式的理解口径
- 选择模式现在不是旧的“hover 卡 + 旧选择测距”。
- 选择模式应当按下面口径理解：
  - 测量展示能力 = 和测量模式同源
  - 编辑能力 = 只保留选中 A 后出现的编辑框
- 如果后续还要继续整理，建议分两轮：
  1. 先验证选择模式复用测量状态机后的稳定性
  2. 稳定后再单独清理 `selectedA / frozenEl / primaryMeasure` 的旧代码

## 今日补充（2026-04-08 上午，测量模式 Step 1-5 已落地）

这部分是今天上午围绕测量模式继续补完的正式规则说明。注意：这轮不是回退旧版，也不是重做 single，而是在昨天已经收口的 single / pair 基线之上，把双元素测量真正拆成稳定的 `separated-pair` 与 `containment` 两类，并补上一个保守版 separated-pair 增强分支。

### 1. 当前测量模式的最终角色定义
- `measureA` 仍然是 click 锁定的主元素。
- `measureB` 仍然是 hover 到的临时目标元素。
- A / B 的职责已经进一步钉死：
  - `A`：只保留局部选中框
  - `B`：负责输出整页参考虚线，并作为 pair 的参考目标
- 这意味着当前测量模式下：
  - A 不再承担整页参考线来源
  - A 不再承担成组圆点 / 控制点 / 旧式对称测量提示
  - B 才是“参考线来源”

### 2. Step 1 实际落下的规则：A / B 显示归属与生命周期
- click A 后：
  - 页面上只保留 A 的局部框
  - A 的 single persistent 继续显示
  - A 的 pair 相关整页 guide 不再出现
- hover B 后：
  - `selectB` 直接跟 `state.measureB`
  - B 输出整页参考虚线
  - 在此基础上叠加 pair 主测量线
- leave B 后：
  - `measureB` 清空
  - B 的框和整页参考虚线一起消失
  - A 的局部框和 single persistent 保留
- `Esc` / 切回选择 / 关闭测量态 / 销毁时：
  - pair guide 会被清掉
  - `selectA / selectB` 也会立即隐藏，不再只等下一帧 refresh 兜底

### 3. Step 2 实际落下的规则：separated-pair 已改成“主轴直连 B 参考线”
- 现在普通双元素分离关系走 `pairKind === "edge-to-edge"`。
- `pairState` 在 edge-to-edge 分支里补了最小元信息：
  - `horizontalRelation`
  - `verticalRelation`
  - `overlapFlags`
  - `startEdgeMidpoints`
  - `targetGuideSide`
- 主轴判定规则：
  - horizontal 存在真实 gap，则水平为主轴
  - vertical 存在真实 gap，则垂直为主轴
  - overlap 轴仍然不补 `0`
- geometry 规则已经统一成：
  - 水平主轴：
    - 从 A.left / A.right 的边中点出发
    - 直接量到 B.left / B.right 的整页参考虚线
  - 垂直主轴：
    - 从 A.top / A.bottom 的边中点出发
    - 直接量到 B.top / B.bottom 的整页参考虚线
- 这里已经不再使用旧版公共 elbow / 公共拐点。
- horizontal / vertical 是独立 routing：
  - 不再共用一个转折点
  - 不再把两个值挂在同一个转角附近

### 4. Step 3 实际落下的规则：containment 已正式分流
- 当前 containment 单独走 `pairKind === "containment"`。
- containment 会在 `pairState` 里显式带上：
  - `containment.outerEl`
  - `containment.innerEl`
- containment 的渲染已经 short-circuit 掉普通 separated-pair 主线。
- 当前 containment 只表达“内部元素相对外部容器边界的剩余空间”：
  - `inner.left midpoint -> outer.left`
  - `inner.right midpoint -> outer.right`
  - `inner.top midpoint -> outer.top`
  - `inner.bottom midpoint -> outer.bottom`
- 只显示有解释价值、且值大于 0 的边。
- 当前明确禁止的旧错误：
  - containment 场景里，不允许再冒出一条普通 separated-pair 的中间主实线
  - 不允许同时出现 containment inset 线和 edge-to-edge 主轴主线

### 5. Step 4 实际落下的规则：标签避让与清理链路收口
- 标签现在沿用统一胶囊样式，但布局上补了基础避让：
  - 主轴标签先占位
  - 后画的标签让开已占位标签
  - 短主段允许把标签外移，不再死挤在线段中心
- separated-pair 和 containment 都使用这套轻量避让，不另外发明一套 token。
- 清理链路也补齐了：
  - `clearMeasureSelection()` 不只清 layer，也会立即隐藏 `selectA / selectB`
  - `setMeasureTopbarMode(true)` 进入测量态时会先清旧 layer，并清空旧的 B 框
- 这轮的目的不是做花哨布局，而是先保证：
  - 不残留旧 guide
  - 不残留旧框
  - 单元素 single 不退化

### 6. Step 5 实际落下的规则：separated-pair 新增“正交轴严格包含增强”
- 这次新增的是 `separated-pair` 的增强分支，不是 containment。
- 进入条件必须同时满足：
  1. A 和 B 在一个轴上存在真实 gap，这个轴是主轴
  2. 在正交轴上，A 的投影完全落入 B，或 B 的投影完全落入 A
- 注意这里当前只按**严格包含**做：
  - 完全落入才触发
  - 部分交叉暂时不算
  - 这是刻意做的保守稳定版
- 当前增强只允许同时出现：
  - `1` 个主轴值
  - `2` 个正交轴增强值
- 不允许：
  - 补 `0`
  - 四边全开
  - 把 separated-pair 增强误做成 containment

### 7. Step 5 增强的 geometry 口径
- 如果主轴是垂直，且水平方向满足严格包含：
  - 先显示现有垂直主轴值
  - 再补：
    - `A.left midpoint -> B.left guide`
    - `A.right midpoint -> B.right guide`
- 如果主轴是水平，且垂直方向满足严格包含：
  - 先显示现有水平主轴值
  - 再补：
    - `A.top midpoint -> B.top guide`
    - `A.bottom midpoint -> B.bottom guide`
- 这里仍然统一遵守：
  - 起点用 A 的边中点
  - 终点连到 B 的整页参考虚线
  - 不重新引入公共 elbow
  - 不新增另一套颜色 / 线型 / 标签样式

### 8. Step 5 增强的 pairState 元信息
- edge-to-edge 分支目前除了 Step 2 的元信息外，又额外补了：
  - `strictOrthogonalContainment`
  - `enhancedOrthogonalSegments`
- 这两个字段的作用是：
  - 提前在 `pairState` 层判断“是否满足严格正交包含”
  - 提前把最多两个增强段准备好
  - render 层直接消费，不再临时猜几何关系

### 9. 当前必须记住的边界
- 当前测量模式里，pair 已正式分成三层理解：
  - `single persistent`：底层稳定信息
  - `separated-pair`：普通分离元素的主轴距离
  - `containment`：内部元素到外部容器的剩余空间
- 另外又新增了一层 `separated-pair enhancement`：
  - 只有在“主轴 gap + 正交轴严格包含”时才会出现
  - 它是 separated-pair 的增强，不是 containment
- 当前不要做的事：
  - 不要把 partial overlap 也算进 Step 5 增强
  - 不要把 Step 5 增强误塞到 containment
  - 不要回头重拆 single
  - 不要回退 A/B 角色定义

### 10. 接手后优先手测的场景
1. 普通 separated-pair
- 只有一个轴有真实 gap，正交轴只是部分交叉
- 预期：不触发 Step 5 增强，只显示主轴值

2. 垂直主轴 + 水平严格包含
- 预期：
  - 1 个垂直主值
  - 再补 left / right 两个增强值

3. 水平主轴 + 垂直严格包含
- 预期：
  - 1 个水平主值
  - 再补 top / bottom 两个增强值

4. containment
- 预期：
  - 仍只显示 inner -> outer 的剩余空间
  - 不混入 separated-pair 主轴主线
  - 不混入 Step 5 增强值

5. 快速 hover / leave / Esc / 切模式 / 销毁
- 预期：
  - 没有残留 guide
  - 没有残留 selectA / selectB
  - single persistent 不退化

### 11. 当前明确没动的部分
- single 分类与 single 显示语义没动：
  - `resolveMeasureStructureData`
  - `resolveMeasureSingleState`
  - `renderMeasureSinglePersistentGuides`
  - `renderMeasureSingleHoverGuides`
  - `repeated-structure / content-container`
  - `box-leaf / text-leaf`
- 也没动：
  - `selected panel`
  - 非测量模式
  - 记录 / 抽屉 / 导出 / 草稿恢复

### 12. 接手时的建议口径
- 当前 pair 主链路已经不是“实验状态”，而是有明确结构的稳定版：
  - Step 1：A / B 角色与生命周期
  - Step 2：普通 separated-pair 主轴
  - Step 3：containment 分流
  - Step 4：避让与清理
  - Step 5：主轴 gap + 正交轴严格包含增强
- 如果后面继续改，建议优先按下面顺序推进：
  1. 验证 Step 5 在真实页面里的命中率和可读性
  2. 如果命中率不足，再考虑把“严格包含”放宽到“可解释的部分交叉”
  3. 放宽之前，先不要改 containment 和 single
  4. 继续保持 A/B 角色边界清晰，不要回到旧的中心点 / 公共 elbow 方案

## 今日补充（2026-04-08 下午，测量显示层再次收口）

这部分是今天下午继续围绕测量模式做的显示层收口。重点不是改 Step 1-5 的规则判断，而是把 A 内 hover-only、B guide 的显示条件、以及 guide segment 的收口方式重新整理清楚。当前这轮修改全部收在 `visual-qa.js` 里，没有拆文件，也没有动记录 / 抽屉 / 导出 / 草稿恢复 / selected panel / 非测量模式。

### 1. A 内 hover-only 信息已经补回
- `measureA` 锁定后，鼠标移入 A 自己的区域时，会重新显示 A 的 hover-only 信息。
- 这条链路不是 pair，而是回到 single 的 hover-only 语义。
- 当前恢复的 hover-only 内容是按既有 single 口径走的：
  - `text-leaf`：显示字号 / 行高
  - `box-leaf`：显示 content size
  - `structure / content-container`：显示容器宽高
- 这里仍然保留 single persistent：
  - `structure` 的 inset / gap 信息继续常驻
  - `box-leaf` 的 edge padding / inset 继续常驻
  - hover-only 只负责短暂提示，不改 persistent 语义

### 2. pair / hover / single 的边界现在是互斥的
- 鼠标在 A 内且没有进入 B：
  - 只显示 A 的 hover-only 信息
  - 不进入 pair
- 鼠标命中 B：
  - 进入现有 pair / containment / Step 5 增强分支
  - 不再同时显示 A 的 hover-only
- 鼠标离开 A 且未命中 B：
  - A 的 hover-only 消失
  - 只保留 A 的 persistent 信息
- 当前这三个状态是刻意拆开的：
  - `A 内 hover-only`
  - `A -> B pair`
  - `A 外无 B 时只留 persistent`

### 3. B guide 已从“整页参考线”改成“短 segment”
- 现在 hover 到 B 后，不再默认画四条整页 reference guide。
- B guide 改成只画当前 measurement 真正需要的短 segment。
- segment 不是从整页拉满，而是只覆盖当前交接范围。
- 现在的 B guide 不再依赖 full-page 线，而是按当前 pairState 里实际参与的边逐条生成。

### 4. B guide 的分类已经拆开
- 当前 B guide 已经按用途拆成三类：
  - `primary-midpoint`
  - `enhancement-side`
  - `containment`
- `primary-midpoint`
  - 主轴距离的核心解释线
  - 默认保留
  - 不再被 side guides 吞掉
- `enhancement-side`
  - 正交轴上的左右 / 上下剩余空间
  - 是辅助信息
  - 只在有解释价值时保留
- `containment`
  - 容器内外剩余空间的表达
  - 继续走 containment 口径

### 5. B guide 的收口策略已经改成“精确去重”，不是“扩展合并”
- 之前的问题不是数值算错，而是 segment 收口太激进。
- 现在 guide segment 只做精确去重：
  - 同起点、同终点、同方向、同用途的重复段才去掉
  - 不再把同一边上的多个交接点合并成长线
  - 不再把上下左右拼成闭合矩形
- 也就是说，当前 guide 只能保持为“线段”，不能自动闭合成框。
- 这是为了避免：
  - 图一里出现紫色大框
  - 图二里出现几乎重合的一圈外框
  - 图三里出现局部框架化

### 6. B guide 的显示条件现在是“分类 + 解释增益”双层判断
- `primary-midpoint` guide：
  - 现在默认保留
  - 不会因为 side guides 已经存在就被省掉
  - 这保证了“主轴 gap + 正交轴剩余空间”同时存在时，主轴那条核心解释线还在
- `enhancement-side` guide：
  - 继续按冗余判断收口
  - 如果这条 guide 只是重复表达一个已经非常清楚的边界，就可以 suppress
- `containment` guide：
  - 继续按 containment 规则显示
  - 不参与 main / side 的互相替代

### 7. 目前这套显示层已经形成的结论
- 主轴 guide 不再被 side guides 误吞掉。
- side guides 也不会再被合并成整条边，更不会拼成框。
- containment 仍然表达内部元素到外部容器的剩余空间。
- Step 5 的增强值仍然只在满足严格正交包含时出现。
- 当前 pair 主链路的规则没有改，只是显示层变得更干净、更接近竞品的视觉口径。

### 8. 后续如果继续改，建议优先验证的场景
1. 大元素 A 测小元素 B
- 预期：主轴 guide 保留，side guides 如有价值则保留，但不会拼成框。

2. 小元素 A 测大元素 B
- 预期：主轴 guide 仍保留，但如果它只是重复表达一个已经很清楚的边界，display 层应继续保持克制。

3. containment
- 预期：只表达容器内外剩余空间，不额外补一圈几乎重合的虚线框。

4. Step 5 增强
- 预期：主轴 guide 继续保留，enhancement-side 只在有增益时显示，不允许把 segment 合成矩形。

### 9. 当前接手时要记住的边界
- 这轮只收口了测量显示层，不要把它理解成规则本身重写。
- 当前没有改：
  - separated-pair / containment / Step 5 的数值计算
  - A / B 角色
  - 主测量线几何
  - single 的数据语义
- 如果后面继续改，优先顺序仍然是：
  1. 先确认显示层是否还会框化
  2. 再确认主轴 guide 是否在边界复杂时还足够清楚
  3. 最后再考虑是否需要微调 enhancement-side 的抑制条件

## 今日补充（2026-04-07，测量模式收口）

这部分是今天下午围绕测量模式继续做的收口说明。当前重点不是重做单元素测量，而是在现有 single 基础上，把 pair 的交互、视觉和边界统一清楚，方便后续信息同步。

### 1. 当前测量模式的定位
- 顶部“辅助测距”仍然是 `select` 下的测量子态，不是新的顶层 mode。
- 测量模式下，`measureA` 代表当前锁定的主元素。
- `measureB` 现在不再是“第二次点击锁定的 B”，而是临时 hover pair 目标。
- 也就是说，当前交互已经从“click A -> click B”改成了“click A -> hover B”。

### 2. 当前交互规则
- 第一次点击元素：
  - 锁定 `measureA`
  - 进入单元素测量状态
- 鼠标移入其他有效元素：
  - 临时把该元素写成 `measureB`
  - 直接显示 A ↔ B 的 pair 测量
- 鼠标移出 B 或 hover 失效：
  - `measureB` 自动清空
  - 立即回到 single A
- 再点击新的元素：
  - 新元素直接成为新的 `measureA`
  - `measureB` 同步清空
- `Esc`：
  - 清空测量层
  - 保留顶部测量子态
- 切回顶部“选择”：
  - 清空测量态
  - 恢复原有选择链路

### 3. single 和 pair 的分层关系
- single 仍然是底层稳定信息，不会因为 pair 出现而被清掉。
- pair 只做 overlay，叠在 single persistent 之上。
- 当前保留的 single persistent 信息包括：
  - `structure` 的绿色 inset
  - `structure` 的紫色一级 gap
  - `box-leaf` 的 edge padding / inset
- single 的 hover-only 仍按原规则处理：
  - 宽高
  - content size
  - 字号 / 行高
- pair 出现时，不会把这些 persistent 信息抹掉，但也不会把 hover-only 强行常驻化。

### 4. pair 的视觉规则
- pair 主测距段改成实线。
- B 元素只负责向对应位置延长虚线投影，虚线只是辅助，不是主测距段。
- 水平和垂直 pair 统一使用同一套颜色、线宽、标签样式。
- 去掉了 pair 线两端的小短横线 / 端帽。
- 数值胶囊贴在主测距实线上，不贴在投影虚线上。
- overlap 轴不显示 `0` 标签。
- 如果某个轴只是说明投影关系，只保留 B 的虚线投影，不伪造主测距线。
- containment 降级也统一使用同一套 pair 视觉 token，不单独分出另一套风格。

### 5. pair 的数据和判定方式
- 当前 pair 不是依赖“第二次点击后锁死 B”。
- pair 的临时目标来自 hover 命中：
  - `measureA` 固定
  - hover 到有效元素时写入 `measureB`
  - 离开后清空 `measureB`
- pair 计算仍复用现有 edge-to-edge 和 containment 逻辑。
- containment 场景继续保留降级：
  - 祖先 / 子元素关系不画完整 edge-to-edge 双向线
  - 只展示更可解释的 containment 结果

### 6. 当前明确不碰的部分
- 不回头改 single 的分类逻辑：
  - `resolveMeasureSingleState`
  - `resolveMeasureStructureData`
  - `repeated-structure / content-container`
  - `box-leaf / text-leaf`
- 不改 single 的 persistent / hover-only 语义。
- 不改非测量模式。
- 不改 selected panel 旧链路。
- 不动记录、抽屉、导出、草稿恢复。

### 7. 接手时需要记住的结论
- 当前测量模式已经分成两层：
  - single：稳定底层
  - pair：临时 overlay
- pair 的重点是“更干净地表达 A 与 B 的距离关系”，不是重做 single。
- 如果后面继续改，建议优先做 pair 的视觉避让、窄空间标签和 containment 边界验证，不要再回头重拆 single。

## 今日补充（2026-04-06）

这部分是最近一轮围绕 V1.2 继续收口的内容，重点有两块：导出素材链路统一，以及记录浮窗入场体验收敛。

### 1. 导出素材链路现在统一成“带框源画布”
- 记录完成后，截图链路会先基于原始 `sourceCanvas` 合成一份带紫框的 `marked` 画布。
- `shot.thumb` 和 `shot.export` 现在都从同一份带框源画布生成，不再存在“缩略图有框、导出图没框”的分叉。
- HTML 导出不再自己额外叠问题框，直接读 `record.shot.export`。
- HTML 导出前会先做一次 preflight：
  - 先补生成缺失的 `shot.export`
  - 再 build HTML
- 取图顺序现在是：
  - `shot.export`
  - `shot.thumb`
  - 占位态
- 这个改动的目标是：
  - 刷新页面后、重开插件后，已有记录导出尽量仍然有图
  - 避免 HTML 只剩空卡片
  - 避免抽屉预览和 HTML 导出出现不同素材口径

### 2. 记录列表和预览对“已烘焙问题框”的素材做了兜底
- 现在新生成的 `shot.thumb / shot.export` 已经带框。
- 抽屉缩略图和预览弹窗对“已标记过的素材”会关闭运行时 overlay，避免重复画框。
- 旧记录如果还是老素材，仍然会沿用原来的运行时 overlay 兜底。
- 目前需要记住的判断是：
  - 新素材优先看 baked frame
  - 旧素材继续保留 runtime overlay 兼容

### 3. 记录浮窗 composer 的入场体验已经收敛
- composer 现在不再靠“先矮版 loading 浮窗，再切正式版”的方式出现。
- 首屏结构固定为完整壳子：
  - 头部
  - 目标名
  - 分类区
  - 备注区
  - 底部操作区
- “截图处理中...” 改成固定占位，不再靠删节点或换整段结构撑高/塌陷。
- 白色主按钮不再单独晚到补 opacity：
  - 它始终保持最终白色视觉
  - 加载态只影响 `disabled` 和可点性
- composer 现在有统一的入场 class：
  - `.v12-record-composer`
  - `.v12-record-composer.is-open`
- 进入时机是：
  - 先算位置
  - 再挂完整内容
  - 下一帧切 `is-open`

### 4. 当前接手时要记住的边界
- 这轮只收敛了导出素材和 composer 入场体验，没有去重做 drawer、preview、capture restore 或记录数据结构。
- 记录图的几何语义还是原来的：
  - `focusRect` = 问题定位框
  - `shotRect` = 截图上下文框
- 如果后面继续改导出图样式，优先改的是“画框样式参数”，不要把 HTML 层再改回 overlay 叠框。

## 历史补充（2026-04-03，上午，旧入口形态，已被 2026-04-09 顶部重排替换）

这部分是当时围绕 V1.2 记录链路继续做的增量，重点不是重做主流程，而是把当时的记录入口、截图恢复、记录浮窗和裁切边界继续收口。当前实现里，顶部入口已经不再是这段里描述的分裂按钮形态。

### 1. 当时的记录入口是分裂按钮
- 工具栏里的“记录”当时还不是两个顶层按钮。
- 旧结构是：
  - 主按钮“记录”：进入记住的记录子模式
  - 右侧小箭头：打开元素 / 区域子模式菜单
- 这套入口后来已经被 2026-04-09 的固定顶层按钮结构替换。

### 2. 截图完成后的 UI 恢复改成淡入
- 之前截图恢复时，顶部工具栏、记录浮窗等会“瞬间闪回”。
- 现在保留原来的截图时机，只在“截图已拿到原始结果之后、UI 恢复阶段”做很轻的过渡。
- 重点恢复对象：
  - 顶部工具栏
  - 记录浮窗
  - 当时的记录菜单 / 抽屉等 capture-exclude UI
- 恢复方式：
  - 主要是 `opacity: 0 -> 1`
  - 时长约 `150ms`
  - `ease-out`
- 隐藏阶段仍然是立即隐藏，不影响截图时机。
- 这块后面已经补过一次清理，避免 capture-hidden 状态残留把主链路打坏。

### 3. 记录态 cleanup 已统一收口
- 之前发现记录执行一次后，模式链路可能被残留状态打坏。
- 当时的记录模式切换、取消、保存、异常回收都尽量走统一 cleanup。
- 重点清理项包括：
  - 当前记录 mode / 子模式状态
  - `pendingRecord`
  - 区域框选 overlay / 监听 / 临时选框状态
  - `captureInProgress`
  - `suppressHover`
  - 各类临时交互锁
- 目的不是做视觉 polish，而是保证：
  - 记录元素后切回选择，hover / highlight 能恢复
  - 记录区域后还能再次框选
  - 保存 / 取消 / 失败都不会留下半残状态

### 4. 记录浮窗现在优先跟随目标 rect
- 记录浮窗不再默认固定贴页面最右侧。
- 现在优先以当前目标 rect 定位：
  - 元素记录：跟元素 rect 走
  - 区域记录：跟框选区域 rect 走
- 候选方向顺序：
  - 右侧
  - 左侧
  - 下方
  - 上方
  - 实在放不下时再回退到右侧固定位置
- 这个改动主要是为了减少“目标区域”和“备注输入”之间来回看视线的割裂感。

### 5. 目前还没彻底解决的遗留问题：元素超出浏览器边缘时仍会出现空白区
- 这是今天最需要继续接着修的点。
- 现象：
  - 元素记录时，如果 `shotRect` 或上下文贴近 viewport 边缘，最终截图里还是可能出现一大片白区/空白补区。
  - 现在的问题已经不是“截图时机”，而是“最终用于裁切的 rect 仍然没有完全稳定地收敛到真实可见像素范围”。
- 当前修复目标本来是：
  - 不做自动滚动
  - 不做多段拼接
  - 不保留 viewport 外拍不到的部分
  - 如果只是上下文超边，就把上下文往可视区内重分配
- 但真实结果说明这条链路还没完全收干净，说明至少有一处还在按更大的 rect 生成白底画布，或者 viewport 收敛后的 rect 没有真正同步到最终裁切 / 预览映射。

### 6. 这个白区问题接下来建议优先检查的点
1. `normalizeRecordCapture()`
- 看最终 `shotRect` 是否真的被稳定收敛成“当前 viewport 内可承载的 rect”。
- 注意不要只在某个分支里收敛，导致别的路径还是老 rect。

2. `buildShotImagesFromCapture()`
- 看这里是不是还在按更大的 `rect` 申请白底 canvas。
- 重点核对：
  - `visibleIntersection`
  - `baseCanvas`
  - `drawImage` 的源/目标偏移
- 这里最容易把不可见区域硬补成白块。

3. 预览映射
- `getRecordShotSvgHtml()`
- `getRecordFocusOverlayBoxStyle()`
- 这两处要确认是跟新的最终 `shotRect` 走，不是继续按旧 rect 画紫框。

4. 结论先记死
- 这条“元素超出浏览器边缘出现空白”的问题，**当前还没有真正修好**。
- 后面继续改时，建议先把“最终 capture rect、实际裁切 rect、预览 overlay rect”三者的关系重新对齐，再考虑别的优化。

### 今日补充（2026-04-03，晚间，Step 5 收口 + Step 6 导出接通）
今天把 2026-04-02 那轮只做了一半的内容继续补完了，重点是把记录抽屉真正收成可用状态，并把 HTML 导出链路接通。

1. Step 5 抽屉现在已经补齐到“可交付”状态
- 抽屉不再只是数据壳，已经能稳定承载当前页的 Draft / Record。
- 列表结构还是维持头部 + 独立滚动区的拆分，翻多条记录时不会继续把卡片压扁。
- 卡片层已经接上的能力：
  - 分类筛选
  - 备注编辑 / 保存
  - 单条删除
  - 清空本次记录
  - 打开三点菜单做单条操作
- 缩略图点击现在会进入大图预览。
- 预览图优先用 `shot.export`，没有时回退 `shot.thumb`。

2. 记录图的语义现在分得更清楚了
- `shot.thumb`：抽屉里的轻量缩略图，偏列表浏览。
- `shot.export`：给大图预览和 HTML 导出用的正式图。
- 预览打开时如果还没生成 `shot.export`，会先懒生成再展示。
- 这部分逻辑已经和 `warmRecordExport()`、`generateRecordExportFromSource()` 这条链路接上了。

3. Step 6 HTML 导出已经完成接入
- 抽屉里的“导出 HTML”现在不是空入口了，会直接生成并下载 HTML 报告。
- 导出数据源是当前页 Draft，不是单条临时记录。
- 产物里会包含：
  - 页面标题 / 页面 URL
  - 导出时间
  - 记录总数
  - 每条记录的分类、备注、目标名、时间、页面信息、位置摘要
  - 记录图，优先使用 `shot.export`
- 文件下载桥接已经补在 `service_worker.js`，通过 `chrome.downloads.download` 落盘。
- `content-bridge.js` 仍负责把页面脚本和 service worker 的消息桥接起来。
- 这次已经能跑通“点击导出 -> 生成 HTML -> 下载文件”的完整闭环，但样式和版式还没做最后一轮细调。

4. 接手时要注意的状态
- Step 5 和 Step 6 都已经接通，不要再按“导出还没开始”的旧口径理解当前仓库。
- 如果后面还要继续改，优先顺序建议是：
  - 抽屉展示细节
  - 导出 HTML 的版式和信息密度
  - 再考虑要不要补更强的导出预览体验
- 截图主链路和 Draft 持久化现在不要回头重拆。

## V1.1 工作记录（2026-03-30）

### 本次目标
- 将 `hover` 与 `selected` 的职责彻底拆开。
- `hover` 收口为“高亮 + 轻量信息卡”。
- `selected` 收口为“稳定工作面板”，承担完整查看、编辑与测量协作。

### 本次已完成改动
1. Hover Card 收口
- 去掉了 hover 态的大标题“Hover 预览”。
- 去掉了 hover 态“关闭”按钮。
- hover 态不再展示完整属性、不再出现编辑控件、不再混入复杂测量说明。
- hover 卡内容控制为 1 到 2 行：
  - 第一行只保留短名。
  - 第二行只保留一组核心信息。

2. Hover 信息层级重构
- 目标名称改为短名策略，优先顺序：
  - `tag + 语义名（aria-label/title/name/placeholder/alt/data-testid/data-qa）`
  - `tag + 简短 class`
  - `tag + 短文本`
- 避免拼接重复 class，避免显示很长技术串，超长自动省略。
- 页面来源不再作为主信息。
- 仅在 iframe 场景下弱化显示 `iframe` 标签；主页面默认不显示“主页面”。

3. Hover 第二行规则
- 文本类元素：显示 `字号 / 行高`。
- 非文本元素：显示尺寸，如 `282 × 57`。
- 当前实现以“单组核心信息”为准，不再混排更多属性。

4. Selected Panel 定位逻辑拆分
- 选中 A 后，selected panel 不再跟随鼠标持续移动。
- selected panel 与 hover card 已不再共用同一套定位逻辑：
  - `hover`：继续跟手，继续自动避让。
  - `selected`：进入时一次性定位，随后锁定。
- selected 默认会优先参考已有位置；若无可用位置，会优先落到稳定区域，再尝试根据 A 的位置做一次性避让。
- 后续鼠标移动只更新测量内容和引导线，不再推动整个 selected panel。
- 用户仍可手动拖拽 selected panel；拖拽后继续记忆该位置。

### 当前交互约定
- `hover = 快速识别`
- `selected = 完整查看 / 编辑`
- `A + hover target` 测量关系允许动态变化
- 动态变化的是测量内容和页面引导线，不是 selected 面板的位置

### 关键代码位置
- 主逻辑文件：`visual-qa.js`
- hover 短名与轻量信息：
  - `truncateText`
  - `getSemanticName`
  - `shortClassName`
  - `shortHoverLabel`
  - `hoverMeta`
- 面板头部与 hover/selected 外观切换：
  - `updatePanelChrome`
- selected 状态与落位控制：
  - `setSelectedEl`
  - `panelNeedsPlacement`
  - `positionHoverTooltip`
  - `resolveSelectedPanelPosition`
  - `positionSelectedPanel`

### 当前验证结果
- 已执行：
  - `node --check visual-qa.js`
- 结果：
  - 语法检查通过。
- 尚未完成：
  - 浏览器内完整人工走查。
  - 典型页面下 hover 短名质量验证。
  - selected 面板默认停靠体验细调。

### 近期补充（2026-03-31，刚拉下来的内容）
这部分是从最新拉下来的 `visual-qa.js` 里补进来的 V1.1 继续优化点，主要集中在 spacing 展示层：

1. spacing 卡片重做
- `padding` / `margin` / `parentGap` 现在统一走新的 card 样式，不再是旧的单行摘要。
- 每张卡都带一个更直观的预览小块，方便快速区分类型。
- 卡片主行现在优先显示压缩后的摘要值，避免一上来就铺满完整四向数值。
- 展开后才显示更完整的细节信息，整体更适合选中态工作面板。

2. parent gap 也支持展开
- 之前 `parent gap` 只是关系摘要，现在和 `padding`、`margin` 一样可以单独展开查看。
- `spacingExpanded` 里新增了 `parentGap` 状态，对应新的 `toggle-parent-gap`。
- 这样选中容器或可测量元素时，父容器间距信息不再只是“看个结果”，而是能继续往下看细节。

3. spacing 文案与信息密度优化
- 对称值会优先压缩成更短的摘要。
- 单侧值、成对值、四向不一致值会按不同规则展示，减少无意义的长串。
- 展开态新增了更规整的明细区，阅读顺序比旧版更稳定。

4. 这次补充的代码关注点
- 重点改动都在 `visual-qa.js` 的 spacing 渲染与展开逻辑里。
- 相关函数主要包括：
  - `formatSpacingBox`
  - `spacingPreview`
  - `renderSpacingCard`
  - `renderSpacingDetailReadout`
  - `toggle-parent-gap`

### 今日补充（2026-03-31，scrub + selected 能力修正）
这部分是今天继续接着 V1.1 做的增量，不重做 panel 结构，重点是数值拖动和 selected panel 能力判断。

1. selected panel 数值 scrub
- 在 selected panel 的数值输入上补了 `Option/Alt + drag` 的 scrub 能力。
- 首批覆盖字段：`宽`、`高`、`透明度`、`圆角`、`字号`、`行高`。
- 后续又补进了 `padding` / `margin` 的摘要态和四边细项。
- 拖动过程保持 `draft / commit` 分离：
  - 拖动中只改当前字段的 draft 和局部输入显示。
  - `mouseup` 才统一 commit 到样式和 `modifiedProps`。
- 数值显示统一改成整数优先，拖动过程中不再默认保留小数。

2. padding / margin 的 scrub 语义
- `padding` 摘要分成两种：
  - `水平`：同时改 `left/right`
  - `垂直`：同时改 `top/bottom`
- `margin` 摘要按形态判断：
  - 单值摘要：四边同步修改
  - 单边摘要：只改对应边
  - 双值 / 多值摘要：摘要态禁用 scrub，要求展开后改四边细项
- 展开后的 `marginTop / Right / Bottom / Left` 和 `paddingTop / Right / Bottom / Left` 都可以单独 scrub。

3. selected panel 能力模型拆分
- 不再用“文本元素 / 非文本元素”二选一决定整个面板能力。
- 新增了更清晰的能力判断：
  - `hasTextStyles`
  - `hasBoxVisual`
  - `canEditRadius`
  - `canEditFill`
  - `canEditStroke`
- 现在像按钮、菜单项、列表项、标签项这类“带文字的容器”可以同时满足：
  - 显示字体区
  - 允许圆角编辑
  - 允许背景色等盒子外观编辑
- 只有真正“纯文本节点”才会禁用圆角。

4. 这轮改动的代码关注点
- 主要改动都在 `visual-qa.js`。
- 相关函数主要包括：
  - `buildSelectedCapabilities`
  - `hasDirectTextSemantic`
  - `hasBoxSemantic`
  - `describeMarginSummaryScrub`
  - `getBoxSummaryScrubMeta`
  - `summaryInput`
  - `renderSelectedPanel`

### 今日补充（2026-04-01，记录态排查与回退）
今天主要围绕 V1.2 记录态做了几轮交互排查，最终确认问题不是所有页面都触发，而是和宿主页面兼容性强相关。当前代码已经回退到稳定基线，记录态的进一步修复先暂停，后续需要基于页面差异再继续。

1. 记录态问题的阶段性结论
- 有些页面在记录态会出现跳顶、长页截断、页面发灰，有些页面则正常。
- 这说明问题更像“页面兼容性触发条件”，不是整套记录功能逻辑都错了。
- 异常主要集中在两个链路：
  - 元素记录打开新增记录浮窗后，页面跳顶、长页异常、光标丢失。
  - 区域记录一进入框选就异常，页面内容缺失或出现大面积灰层。

2. 已排查到的重点风险点
- `textarea` 自动 focus 本身不是主因，关键更像是记录弹窗在滚动或刷新时被重建，导致焦点丢失。
- 记录态里对宿主页面根节点的样式修改风险很高，尤其是 `body/html` 的滚动锁、`position: fixed`、`overflow` 改写。
- 区域框选 overlay 如果和宿主页面样式、挂载层或背景遮罩耦合过深，就可能触发页面视觉异常。

3. 这一轮做过的最小验证
- 先验证了元素记录链路，重点观察打开浮窗后是否因为刷新重建导致 textarea 丢焦。
- 记录弹窗的 textarea 已改成 `focus({ preventScroll: true })`，并尽量复用同一个输入节点，避免滚动时重新创建 DOM。
- 对“滚动锁”做过多次实验后，最终回到了稳定基线，没有保留侵入宿主页面布局的实现。

4. 当前状态
- 当前仓库代码已回到稳定基线，选择 / 编辑 / 辅助测距主链路恢复优先级最高。
- 记录态的兼容性修复先不继续往下推，后续建议先在“正常页 vs 异常页”之间继续做更细的差异定位，再决定最小修复点。

5. 后续建议
- 优先找出异常页和正常页在滚动容器、祖先上下文、布局锁定上的差异。
- 记录态修复应尽量保持最小变更，避免再次影响 V1.1 主链路。
- 如果后面继续做记录态，建议先单独验证元素记录，再单独验证区域记录，不要一起大改。

### 今日补充（2026-04-02 上午，V1.2 Step 4 验证完成 + Step 5 初版接入）
今天上午主要推进了 V1.2 的 Step 4 和 Step 5 前半段，重点不是重做交互，而是把记录链路从“可演示”推进到“真实数据可流转”。

1. Step 4 已落地并完成验证
- 正式截图链路已接通，不再只停留在记录浮窗层。
- 记录对象里已经会写入：
  - `record.shot.thumb`
  - `record.shot.export`
  - `record.capture.rect`
  - `record.capture.scroll`
- Draft 持久化与恢复已接通：
  - 同页自动保存
  - 刷新恢复
  - 关闭插件再打开恢复
  - 关闭浏览器后再次打开恢复最近草稿
- 截图失败时不会阻断记录保存，失败链路会回退到可保存的兜底状态。

2. Step 4 涉及的代码范围
- `manifest.json`
  - 增加了截图需要的权限。
- `service_worker.js`
  - 新增可见视口截图桥接。
- `visual-qa.js`
  - 增加截图请求、裁切、双尺寸生成、Draft 回写、失败兜底。

3. Step 5 当前已做到的部分
- 右侧记录抽屉已经从“占位壳”升级为真实数据抽屉。
- 抽屉数据来源已经直接读取当前页面 Draft / Record，不再是 mock。
- 当前已接入的能力：
  - 顶部右侧数字入口作为全局入口打开 / 收起抽屉
  - 打开 / 关闭抽屉不再强制切换回选择模式，drawer 与 mode 独立
  - 基础分类筛选
  - 备注编辑并直接回写 Draft
  - 单条删除并直接回写 Draft
  - “清空本次”会清掉当前 `pageKey` 对应的 Draft
  - “导出 HTML”按钮已经接通真正导出，会直接生成并下载 HTML 报告

4. 今天上午确认通过的交互点
- 在选择模式下打开 drawer，不会切换模式。
- 在当前记录态下打开 drawer，也不会切换模式，当前记录子态保持不变。
- 新增记录弹窗输入焦点稳定，滚动后不需要重新点击输入框。
- 记录主链路没有再破坏 V1.1 的选择 / 编辑 / 辅助测距主链路。

5. 当前已确认存在的问题
- 记录卡数量变多后，抽屉内的卡片有被压缩的情况，滚动结构还需要继续收口。
- 卡片缩略图当前更像封面图，裁切偏重，不利于快速确认到底截了哪个问题区域。
- 卡片信息目前偏杂，仍带有页面信息和 `x / y / w / h` 坐标，信息噪音较大。
- 今天还尝试过一版“缩略图预览 / 大图预览”的交互收口，但这轮没有保留到当前基线，原因是数据源和展示口径还没完全收清，继续保留会增加混乱。

6. 当前代码停留阶段
- V1.2 现在可以认为处在：
  - Step 4 已完成并验收通过
  - Step 5 已完成“真实 Draft / Record 接入版”
  - Step 5 的抽屉体验收口还没有完全磨平
- 当前已经进入 Step 6：
  - HTML 导出已经接通，但导出样式和信息密度还没继续精调
  - 不要把“功能已接通”误判成“版式已最终定稿”

7. 接下来最应该做的事
- 继续收口 Step 5，同时把 Step 6 的导出版式继续磨平，不要重拆截图链路。
- 下一步建议按下面顺序推进：
  1. 先修抽屉滚动结构
  - 目标是头部固定、列表独立滚动、卡片高度稳定。
  2. 再修缩略图展示策略
  - 优先保证能看清记录区域，而不是铺满容器。
  3. 再考虑是否补轻量大图预览
  - 如果做，必须先明确真实数据源优先级，再决定是否保留。
  4. 抽屉信息层级继续精简
  - Step 5 阶段应优先服务“回看与管理记录”，不要过早做成调试数据面板。

8. 接手时的注意事项
- 不要回退 V1.1 已完成结构。
- 不要重做 Step 4 的截图与存储链路。
- 如果继续改 Step 5，建议每次只收一个体验点，不要同时动抽屉结构、截图规则和导出细节。

### 今日续补（2026-04-02 下午至晚间，Step 4 收口 + Step 5 预览链路排查）
今天后半段继续围绕 V1.2 的记录链路做收口，但重点已经从“能不能出图”转到“出图口径是否准确、预览是否可信”。这部分要和上午的记录分开看：Step 4 主链路已经基本跑通，后面更多是在修 Step 5 的展示层和数据语义。

1. Step 4 下午确认收口的内容
- 截图链路已确认支持滚动后的页面，不再只在首屏正常：
  - 首屏元素记录可出图
  - 首屏区域记录可出图
  - 下滑后元素记录可出图
  - 下滑后区域记录可出图
- 问题根因已经定位过一次：
  - 之前把 viewport rect 和 document rect 混用了
  - 修完后截图裁切统一按 document/page 坐标记录，再结合截图时的 scroll 去求当前视口交集
- 为避免把插件自身 UI 截进图里，截图前后增加了“临时隐藏插件 UI”的清洁链路：
  - 顶部工具条
  - 记录浮窗
  - 抽屉
  - hover / highlight / live selection / measure guides
  - 其他插件注入层
  - 流程是：隐藏 -> 等 1 到 2 帧 -> 截图 -> 恢复

2. 记录数据结构已升级为双层语义
- 新记录不再只写一个 `capture.rect`，而是区分：
  - `capture.focusRect`
  - `capture.shotRect`
  - `capture.scroll`
- 语义约定：
  - `focusRect` = 用户原始问题框，必须忠实保留
  - `shotRect` = 真正用于截图裁切的上下文框
  - `scroll` = 本次截图时的滚动偏移
- 旧记录兼容口径：
  - 如果旧记录只有 `capture.rect` 且已有旧图
  - 当前先按 `focusRect = shotRect = capture.rect` 兼容显示
  - 不默认强制重生成旧图

3. Step 5 抽屉侧今天已接过的内容
- 抽屉列表滚动结构已经做过一轮收口：
  - 头部和列表区拆开
  - 列表区单独滚动
  - 卡片不再随着数量增多被持续压缩
- 缩略图展示改成“完整可见优先”，不是强裁切铺满：
  - 更偏向 `contain`
  - 优先帮助确认截图对不对，而不是做封面图
- 点击缩略图可打开大图预览，数据源优先级是：
  - `shot.export`
  - 没有时再回退 `shot.thumb`

4. shotRect / 上下文规则今天也做过一轮重构
- 对记录图的理解已经从“用户框多大就截多大”改成“两层”：
  - `focusRect` 表示问题定位
  - `shotRect` 表示截图上下文
- 元素记录和区域记录都补过多轮最小取景和上下文规则，目标是：
  - 小目标能看出所在模块
  - 中大目标不要只把问题点单独拍大
  - 但仍然维持中小图，不做整页图
- 当前这一块已经不是今天最主要的遗留点，后续如继续调，建议只轻调单个分档，不要再重开“智能识别 / 模块取景 / 回页面定位”。

5. 今天重点暴露出来、目前还没有完全收干净的问题
- 主要遗留已经集中到 preview modal，不在截图主链路：
  1. 大图预览外层 shell 在某些超长横图场景下仍然偏大
  - 表现是深色壳子没有完全跟随真实图片收缩，右侧可能仍出现不合理空白
  2. 预览层的正常分支和失败分支虽然已开始拆开，但还需要继续看真实运行结果
  - 目标应该是：
    - 有图 = 图片 + overlay
    - 无图 / 加载失败 = 纯占位态
  - 当前需要继续验证有没有残留“无图但还渲染大壳子 / 紫框”的情况
  3. focusRect 在大图预览里的还原体感仍需继续核对
  - 产品口径已经明确：
    - 预览展示的是 `shotRect` 截图
    - 但紫框必须忠实等比还原用户原始框选 `focusRect`
  - 不能为了铺满预览去改变原始框选边界体感

6. preview modal 当前代码口径
- 这一轮已经改成由图片原始尺寸去计算预览显示尺寸：
  - 按最大展示边界做等比缩放
  - overlay 也按缩放后的 `displayWidth / displayHeight` 去映射
- 同时给导出图补了长边兜底，避免超大图异常：
  - `export` 长边上限已提到 `1600`
  - `thumb` 长边维持轻量级别
- 但注意：
  - 这部分虽然已经接了计算逻辑和日志
  - 真实运行下的视觉体验还没有完全验收通过
  - 当前不能把 preview modal 标成“已完成”

7. 当前日志口径
- 预览层排查日志使用：
  - `[visual-qa][v1.2][preview-layout-real]`
- 当前日志会打印：
  - 图片原始尺寸
  - 预览最大边界
  - 计算出的 scale / displayWidth / displayHeight
  - `previewBody / previewShell / imageStage / img / overlay` 的真实 DOM 尺寸
  - 是否有预览源
  - 图片是否加载成功
  - 当前命中的渲染分支（`image` / `placeholder`）
- 如果继续排查 preview，优先看这条日志，不要先继续改 shotRect。

8. 当前阶段判断
- 到 2026-04-02 晚间为止，可以把 V1.2 当前状态理解为：
  - Step 4 截图与持久化链路已基本稳定
  - Step 5 的抽屉基础能力已接通
  - Step 5 的 preview modal 仍在排查期，还不能当成最终版
- 因此接手时建议明确分层：
  - 截图链路：先别乱动
  - `focusRect / shotRect` 数据语义：先别推翻
  - 当前主要工作面：只修 preview modal 的真实展示链路

9. 如果下一位继续接，建议顺序
- 先用真实记录在浏览器里复验 preview modal：
  - 普通图
  - 超长横图
  - 超长竖图
  - 无图 / 加载失败
- 先读 `[visual-qa][v1.2][preview-layout-real]` 日志，再决定是不是继续改 preview shell / imageStage / overlay
- 不要一边排 preview，一边再去改 shotRect 分档
- 不要重新打开智能取景、模块识别、回页面定位这些高风险方向
- HTML 导出已经接通，不要再按“还没开始”的旧口径理解当前仓库

### 换电脑后建议先做的事
1. 先在浏览器里验证 3 类页面
- 常规表单页
- 图文混排页
- 含 iframe 的页面

2. 重点确认以下体验
- hover 是否始终保持 1 到 2 行，不回退成半个完整面板。
- hover 短名是否仍会出现过长技术串。
- selected 后面板是否稳定，不再跟随鼠标。
- `selected A + hover target` 的测量内容是否正常动态更新。

3. 下一步优先建议
- 继续收 selected panel 的默认停靠策略，考虑是否固定成更明确的右侧工作面板。
- 继续优化 hover 短名策略，减少工程化 class 暴露。
- 补一份面向使用者的简短说明，明确 `hover` 与 `selected` 的分工。

## V1.0.0 发布说明
- 这是 `视觉走查助手` 的首个正式发布基线，对应 `manifest.json` 中的 `1.0.0`。
- 本版聚焦网页视觉走查的核心闭环：高亮、冻结、测距、样式临时编辑、面板固定/收起。
- 当前实现默认按单页主框架运行，不额外扩展 iframe 兼容。
- 本仓库不再沿用临时表述，后续变化将以新版本记录。

## 项目定位
- 这是一个 **Chrome MV3 扩展**，用于网页视觉走查。
- 入口逻辑：点击扩展图标后，由 `service_worker.js` 注入 `visual-qa.js` 到当前标签页。
- 全部能力本地执行，不依赖后端服务。

## V1.0.0 核心能力
1. 元素高亮与实时信息面板
- 鼠标悬停元素时，绘制高亮框并展示尺寸、字体、颜色、内外边距等信息。
- 信息面板默认跟随鼠标，避免遮挡时会自动避让。

2. 冻结模式（点击或热键）
- 可将当前元素冻结，持续查看该元素数据。
- 冻结后可基于鼠标位置显示四侧间距引导线（padding / margin / parent gap）。

3. 双元素测距
- 开启测距模式后，依次选择 A/B 两个元素。
- 展示水平距离与垂直距离，并在页面绘制测距线。

4. 样式临时编辑（冻结态）
- 冻结后，面板里的部分字段可直接编辑（如宽高、字号、颜色、圆角、padding/margin）。
- 编辑会写入元素 `inline style`，并支持一键重置清除本次改动。

5. 面板交互增强
- 支持面板固定（Pin）、拖拽。
- 支持收起为悬浮按钮，悬浮按钮可拖动并自动吸附左右边。

6. 快捷键
- `M` 测距开关
- `F` 冻结/解冻
- `C` 清空测距
- `R` 清除样式改动
- `P` 固定/取消固定面板
- `B` 收起/展开面板
- `Esc` 只清当前临时态或当前测量态，不退出工具

## 核心逻辑（代码层）
1. 注入与生命周期
- `manifest.json` 声明 `activeTab` + `scripting` 权限。
- `service_worker.js` 监听 `chrome.action.onClicked`，注入 `visual-qa.js`。
- `visual-qa.js` 以 IIFE 运行；若当前 tab 已存在实例，则再次点击扩展图标会销毁旧实例（相当于开关）。
- 插件当前只允许通过扩展图标点击销毁，不再支持 `Esc` 退出。

2. 全局状态驱动
- `state` 管理 hover 元素、冻结元素、测距 A/B、面板位置、拖拽状态、收起状态等。
- 所有 UI 刷新统一走 `schedule() -> requestAnimationFrame -> refresh()`，避免高频事件导致抖动和重复渲染。

3. 事件系统
- 文档级监听：`mousemove`、`click`、`keydown`、`mouseup`。
- 面板级监听：输入、按钮点击、拖拽。
- 通过 `fromPoint()` 过滤掉工具自身 DOM，确保只选中页面业务元素。

4. 视图层组织
- 独立层：高亮层、间距层、测距层、A/B 选择框、Tooltip 面板、悬浮按钮。
- `renderTooltip()` 负责拼装面板内容；`addSpacingGuides()` / `addMeasureGuides()` 负责绘制线段与标注。

5. 退出与清理
- `destroy()` 统一解绑事件、移除注入节点、取消 RAF、清理全局标记，避免内存泄漏。
- 当前 `destroy()` 的触发源已经收敛到扩展图标点击，不再允许页面内 `Esc` 直接触发。

## 换设备直接使用（最短路径）
1. 加载未打包扩展（推荐开发调试）
- 打开 Chrome：`chrome://extensions/`
- 开启“开发者模式”
- 选择“加载已解压的扩展程序”
- 选择目录：`D:\Codex_Projects\Zoucha_Shijue`
- 打开任意网页，点击扩展图标即可启动/关闭工具

2. 直接使用已有压缩包（分发）
- 仓库内已有：`visual-qa-extension.zip`
- 若你后续修改了源码，建议重新打包（见下节），确保 zip 与源码一致。

## 修改后重新打包（Windows）
在项目根目录执行：

```powershell
Compress-Archive -Path manifest.json,service_worker.js,visual-qa.js,icons -DestinationPath visual-qa-extension.zip -Force
```

## Store 素材与脚本
- 上架文案：`store-listing/listing-zh.md`
- 隐私政策：`store-listing/privacy-policy.md`
- 宣传图：`store-listing/promo-small.png`、`store-listing/promo-marquee.png`
- 截图脚本：`scripts/capture_store_shots.js`

运行截图脚本（需 Node + Playwright）：

```powershell
node scripts/capture_store_shots.js
```

输出目录：`store-listing/screenshots`

## 现阶段已知边界
- 测量与样式读取基于浏览器计算样式，复杂布局（transform、特殊 stacking/context）下可能与视觉感知有差异。
- 截图脚本的 cookie 弹窗处理是常见按钮匹配，目标站点变化时可能需要补 selector。
- 当前无持久化存储；刷新页面后状态重置。

## 后续可选迭代
1. 增加“复制测量结果”按钮，便于直接贴到缺陷单。
2. 增加简单设置项（快捷键映射、颜色主题、默认收起状态）。
3. 补一份面向非开发同学的 `README.md`（动图 + 10 秒上手）。
