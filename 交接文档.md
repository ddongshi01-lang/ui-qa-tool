# 视觉走查助手交接文档

## V1.1 工作记录（2026-03-30）

### 本次目标
- 将 `hover` 与 `selected` 的职责彻底拆开。
- `hover` 收口为“高亮 + 轻量信息卡”。
- `selected` 收口为“稳定工作面板”，承担完整查看、编辑与测量协作。

### 本次已完成改动
1. Hover Card 收口
- 去掉了 hover 态的大标题“Hover 预览”。
- 去掉了 hover 态“关闭”按钮。
- hover 态不再展示完整属性、不再出现编辑控件、不再混入复杂测量说明。
- hover 卡内容控制为 1 到 2 行：
  - 第一行只保留短名。
  - 第二行只保留一组核心信息。

2. Hover 信息层级重构
- 目标名称改为短名策略，优先顺序：
  - `tag + 语义名（aria-label/title/name/placeholder/alt/data-testid/data-qa）`
  - `tag + 简短 class`
  - `tag + 短文本`
- 避免拼接重复 class，避免显示很长技术串，超长自动省略。
- 页面来源不再作为主信息。
- 仅在 iframe 场景下弱化显示 `iframe` 标签；主页面默认不显示“主页面”。

3. Hover 第二行规则
- 文本类元素：显示 `字号 / 行高`。
- 非文本元素：显示尺寸，如 `282 × 57`。
- 当前实现以“单组核心信息”为准，不再混排更多属性。

4. Selected Panel 定位逻辑拆分
- 选中 A 后，selected panel 不再跟随鼠标持续移动。
- selected panel 与 hover card 已不再共用同一套定位逻辑：
  - `hover`：继续跟手，继续自动避让。
  - `selected`：进入时一次性定位，随后锁定。
- selected 默认会优先参考已有位置；若无可用位置，会优先落到稳定区域，再尝试根据 A 的位置做一次性避让。
- 后续鼠标移动只更新测量内容和引导线，不再推动整个 selected panel。
- 用户仍可手动拖拽 selected panel；拖拽后继续记忆该位置。

### 当前交互约定
- `hover = 快速识别`
- `selected = 完整查看 / 编辑`
- `A + hover target` 测量关系允许动态变化
- 动态变化的是测量内容和页面引导线，不是 selected 面板的位置

### 关键代码位置
- 主逻辑文件：`visual-qa.js`
- hover 短名与轻量信息：
  - `truncateText`
  - `getSemanticName`
  - `shortClassName`
  - `shortHoverLabel`
  - `hoverMeta`
- 面板头部与 hover/selected 外观切换：
  - `updatePanelChrome`
- selected 状态与落位控制：
  - `setSelectedEl`
  - `panelNeedsPlacement`
  - `positionHoverTooltip`
  - `resolveSelectedPanelPosition`
  - `positionSelectedPanel`

### 当前验证结果
- 已执行：
  - `node --check visual-qa.js`
- 结果：
  - 语法检查通过。
- 尚未完成：
  - 浏览器内完整人工走查。
  - 典型页面下 hover 短名质量验证。
  - selected 面板默认停靠体验细调。

### 近期补充（2026-03-31，刚拉下来的内容）
这部分是从最新拉下来的 `visual-qa.js` 里补进来的 V1.1 继续优化点，主要集中在 spacing 展示层：

1. spacing 卡片重做
- `padding` / `margin` / `parentGap` 现在统一走新的 card 样式，不再是旧的单行摘要。
- 每张卡都带一个更直观的预览小块，方便快速区分类型。
- 卡片主行现在优先显示压缩后的摘要值，避免一上来就铺满完整四向数值。
- 展开后才显示更完整的细节信息，整体更适合选中态工作面板。

2. parent gap 也支持展开
- 之前 `parent gap` 只是关系摘要，现在和 `padding`、`margin` 一样可以单独展开查看。
- `spacingExpanded` 里新增了 `parentGap` 状态，对应新的 `toggle-parent-gap`。
- 这样选中容器或可测量元素时，父容器间距信息不再只是“看个结果”，而是能继续往下看细节。

3. spacing 文案与信息密度优化
- 对称值会优先压缩成更短的摘要。
- 单侧值、成对值、四向不一致值会按不同规则展示，减少无意义的长串。
- 展开态新增了更规整的明细区，阅读顺序比旧版更稳定。

4. 这次补充的代码关注点
- 重点改动都在 `visual-qa.js` 的 spacing 渲染与展开逻辑里。
- 相关函数主要包括：
  - `formatSpacingBox`
  - `spacingPreview`
  - `renderSpacingCard`
  - `renderSpacingDetailReadout`
  - `toggle-parent-gap`

### 今日补充（2026-03-31，scrub + selected 能力修正）
这部分是今天继续接着 V1.1 做的增量，不重做 panel 结构，重点是数值拖动和 selected panel 能力判断。

1. selected panel 数值 scrub
- 在 selected panel 的数值输入上补了 `Option/Alt + drag` 的 scrub 能力。
- 首批覆盖字段：`宽`、`高`、`透明度`、`圆角`、`字号`、`行高`。
- 后续又补进了 `padding` / `margin` 的摘要态和四边细项。
- 拖动过程保持 `draft / commit` 分离：
  - 拖动中只改当前字段的 draft 和局部输入显示。
  - `mouseup` 才统一 commit 到样式和 `modifiedProps`。
- 数值显示统一改成整数优先，拖动过程中不再默认保留小数。

2. padding / margin 的 scrub 语义
- `padding` 摘要分成两种：
  - `水平`：同时改 `left/right`
  - `垂直`：同时改 `top/bottom`
- `margin` 摘要按形态判断：
  - 单值摘要：四边同步修改
  - 单边摘要：只改对应边
  - 双值 / 多值摘要：摘要态禁用 scrub，要求展开后改四边细项
- 展开后的 `marginTop / Right / Bottom / Left` 和 `paddingTop / Right / Bottom / Left` 都可以单独 scrub。

3. selected panel 能力模型拆分
- 不再用“文本元素 / 非文本元素”二选一决定整个面板能力。
- 新增了更清晰的能力判断：
  - `hasTextStyles`
  - `hasBoxVisual`
  - `canEditRadius`
  - `canEditFill`
  - `canEditStroke`
- 现在像按钮、菜单项、列表项、标签项这类“带文字的容器”可以同时满足：
  - 显示字体区
  - 允许圆角编辑
  - 允许背景色等盒子外观编辑
- 只有真正“纯文本节点”才会禁用圆角。

4. 这轮改动的代码关注点
- 主要改动都在 `visual-qa.js`。
- 相关函数主要包括：
  - `buildSelectedCapabilities`
  - `hasDirectTextSemantic`
  - `hasBoxSemantic`
  - `describeMarginSummaryScrub`
  - `getBoxSummaryScrubMeta`
  - `summaryInput`
  - `renderSelectedPanel`

### 今日补充（2026-04-01，记录态排查与回退）
今天主要围绕 V1.2 记录态做了几轮交互排查，最终确认问题不是所有页面都触发，而是和宿主页面兼容性强相关。当前代码已经回退到稳定基线，记录态的进一步修复先暂停，后续需要基于页面差异再继续。

1. 记录态问题的阶段性结论
- 有些页面在记录态会出现跳顶、长页截断、页面发灰，有些页面则正常。
- 这说明问题更像“页面兼容性触发条件”，不是整套记录功能逻辑都错了。
- 异常主要集中在两个链路：
  - 元素记录打开新增记录浮窗后，页面跳顶、长页异常、光标丢失。
  - 区域记录一进入框选就异常，页面内容缺失或出现大面积灰层。

2. 已排查到的重点风险点
- `textarea` 自动 focus 本身不是主因，关键更像是记录弹窗在滚动或刷新时被重建，导致焦点丢失。
- 记录态里对宿主页面根节点的样式修改风险很高，尤其是 `body/html` 的滚动锁、`position: fixed`、`overflow` 改写。
- 区域框选 overlay 如果和宿主页面样式、挂载层或背景遮罩耦合过深，就可能触发页面视觉异常。

3. 这一轮做过的最小验证
- 先验证了元素记录链路，重点观察打开浮窗后是否因为刷新重建导致 textarea 丢焦。
- 记录弹窗的 textarea 已改成 `focus({ preventScroll: true })`，并尽量复用同一个输入节点，避免滚动时重新创建 DOM。
- 对“滚动锁”做过多次实验后，最终回到了稳定基线，没有保留侵入宿主页面布局的实现。

4. 当前状态
- 当前仓库代码已回到稳定基线，选择 / 编辑 / 辅助测距主链路恢复优先级最高。
- 记录态的兼容性修复先不继续往下推，后续建议先在“正常页 vs 异常页”之间继续做更细的差异定位，再决定最小修复点。

5. 后续建议
- 优先找出异常页和正常页在滚动容器、祖先上下文、布局锁定上的差异。
- 记录态修复应尽量保持最小变更，避免再次影响 V1.1 主链路。
- 如果后面继续做记录态，建议先单独验证元素记录，再单独验证区域记录，不要一起大改。

### 今日补充（2026-04-02 上午，V1.2 Step 4 验证完成 + Step 5 初版接入）
今天上午主要推进了 V1.2 的 Step 4 和 Step 5 前半段，重点不是重做交互，而是把记录链路从“可演示”推进到“真实数据可流转”。

1. Step 4 已落地并完成验证
- 正式截图链路已接通，不再只停留在记录浮窗层。
- 记录对象里已经会写入：
  - `record.shot.thumb`
  - `record.shot.export`
  - `record.capture.rect`
  - `record.capture.scroll`
- Draft 持久化与恢复已接通：
  - 同页自动保存
  - 刷新恢复
  - 关闭插件再打开恢复
  - 关闭浏览器后再次打开恢复最近草稿
- 截图失败时不会阻断记录保存，失败链路会回退到可保存的兜底状态。

2. Step 4 涉及的代码范围
- `manifest.json`
  - 增加了截图需要的权限。
- `service_worker.js`
  - 新增可见视口截图桥接。
- `visual-qa.js`
  - 增加截图请求、裁切、双尺寸生成、Draft 回写、失败兜底。

3. Step 5 当前已做到的部分
- 右侧记录抽屉已经从“占位壳”升级为真实数据抽屉。
- 抽屉数据来源已经直接读取当前页面 Draft / Record，不再是 mock。
- 当前已接入的能力：
  - 顶部“记录列表”作为全局入口打开 / 收起抽屉
  - 打开 / 关闭抽屉不再强制切换回选择模式
  - 基础分类筛选
  - 备注编辑并直接回写 Draft
  - 单条删除并直接回写 Draft
  - “清空本次”会清掉当前 `pageKey` 对应的 Draft
  - “导出 HTML”按钮只保留入口，真正导出还没做

4. 今天上午确认通过的交互点
- 在选择模式下打开记录列表，不会切换模式。
- 在记录模式下打开记录列表，不会切换模式，记录子类型也会保持。
- 新增记录弹窗输入焦点稳定，滚动后不需要重新点击输入框。
- 记录主链路没有再破坏 V1.1 的选择 / 编辑 / 辅助测距主链路。

5. 当前已确认存在的问题
- 记录卡数量变多后，抽屉内的卡片有被压缩的情况，滚动结构还需要继续收口。
- 卡片缩略图当前更像封面图，裁切偏重，不利于快速确认到底截了哪个问题区域。
- 卡片信息目前偏杂，仍带有页面信息和 `x / y / w / h` 坐标，信息噪音较大。
- 今天还尝试过一版“缩略图预览 / 大图预览”的交互收口，但这轮没有保留到当前基线，原因是数据源和展示口径还没完全收清，继续保留会增加混乱。

6. 当前代码停留阶段
- V1.2 现在可以认为处在：
  - Step 4 已完成并验收通过
  - Step 5 已完成“真实 Draft / Record 接入版”
  - Step 5 的抽屉体验收口还没有完成
- 明确还没有进入 Step 6：
  - HTML 导出逻辑还未开始接
  - 不要把当前代码误判成已经做完导出

7. 接下来最应该做的事
- 继续收口 Step 5，不要跳 Step 6。
- 下一步建议按下面顺序推进：
  1. 先修抽屉滚动结构
  - 目标是头部固定、列表独立滚动、卡片高度稳定。
  2. 再修缩略图展示策略
  - 优先保证能看清记录区域，而不是铺满容器。
  3. 再考虑是否补轻量大图预览
  - 如果做，必须先明确真实数据源优先级，再决定是否保留。
  4. 抽屉信息层级继续精简
  - Step 5 阶段应优先服务“回看与管理记录”，不要过早做成调试数据面板。

8. 接手时的注意事项
- 不要回退 V1.1 已完成结构。
- 不要重做 Step 4 的截图与存储链路。
- 不要提前进入 Step 6。
- 如果继续改 Step 5，建议每次只收一个体验点，不要同时动抽屉结构、截图规则和导出口径。

### 换电脑后建议先做的事
1. 先在浏览器里验证 3 类页面
- 常规表单页
- 图文混排页
- 含 iframe 的页面

2. 重点确认以下体验
- hover 是否始终保持 1 到 2 行，不回退成半个完整面板。
- hover 短名是否仍会出现过长技术串。
- selected 后面板是否稳定，不再跟随鼠标。
- `selected A + hover target` 的测量内容是否正常动态更新。

3. 下一步优先建议
- 继续收 selected panel 的默认停靠策略，考虑是否固定成更明确的右侧工作面板。
- 继续优化 hover 短名策略，减少工程化 class 暴露。
- 补一份面向使用者的简短说明，明确 `hover` 与 `selected` 的分工。

## V1.0.0 发布说明
- 这是 `视觉走查助手` 的首个正式发布基线，对应 `manifest.json` 中的 `1.0.0`。
- 本版聚焦网页视觉走查的核心闭环：高亮、冻结、测距、样式临时编辑、面板固定/收起。
- 当前实现默认按单页主框架运行，不额外扩展 iframe 兼容。
- 本仓库不再沿用临时表述，后续变化将以新版本记录。

## 项目定位
- 这是一个 **Chrome MV3 扩展**，用于网页视觉走查。
- 入口逻辑：点击扩展图标后，由 `service_worker.js` 注入 `visual-qa.js` 到当前标签页。
- 全部能力本地执行，不依赖后端服务。

## V1.0.0 核心能力
1. 元素高亮与实时信息面板
- 鼠标悬停元素时，绘制高亮框并展示尺寸、字体、颜色、内外边距等信息。
- 信息面板默认跟随鼠标，避免遮挡时会自动避让。

2. 冻结模式（点击或热键）
- 可将当前元素冻结，持续查看该元素数据。
- 冻结后可基于鼠标位置显示四侧间距引导线（padding / margin / parent gap）。

3. 双元素测距
- 开启测距模式后，依次选择 A/B 两个元素。
- 展示水平距离与垂直距离，并在页面绘制测距线。

4. 样式临时编辑（冻结态）
- 冻结后，面板里的部分字段可直接编辑（如宽高、字号、颜色、圆角、padding/margin）。
- 编辑会写入元素 `inline style`，并支持一键重置清除本次改动。

5. 面板交互增强
- 支持面板固定（Pin）、拖拽。
- 支持收起为悬浮按钮，悬浮按钮可拖动并自动吸附左右边。

6. 快捷键
- `M` 测距开关
- `F` 冻结/解冻
- `C` 清空测距
- `R` 清除样式改动
- `P` 固定/取消固定面板
- `B` 收起/展开面板
- `Esc` 退出工具

## 核心逻辑（代码层）
1. 注入与生命周期
- `manifest.json` 声明 `activeTab` + `scripting` 权限。
- `service_worker.js` 监听 `chrome.action.onClicked`，注入 `visual-qa.js`。
- `visual-qa.js` 以 IIFE 运行；若已存在实例，则再次点击会销毁旧实例（相当于开关）。

2. 全局状态驱动
- `state` 管理 hover 元素、冻结元素、测距 A/B、面板位置、拖拽状态、收起状态等。
- 所有 UI 刷新统一走 `schedule() -> requestAnimationFrame -> refresh()`，避免高频事件导致抖动和重复渲染。

3. 事件系统
- 文档级监听：`mousemove`、`click`、`keydown`、`mouseup`。
- 面板级监听：输入、按钮点击、拖拽。
- 通过 `fromPoint()` 过滤掉工具自身 DOM，确保只选中页面业务元素。

4. 视图层组织
- 独立层：高亮层、间距层、测距层、A/B 选择框、Tooltip 面板、悬浮按钮。
- `renderTooltip()` 负责拼装面板内容；`addSpacingGuides()` / `addMeasureGuides()` 负责绘制线段与标注。

5. 退出与清理
- `destroy()` 统一解绑事件、移除注入节点、取消 RAF、清理全局标记，避免内存泄漏。

## 换设备直接使用（最短路径）
1. 加载未打包扩展（推荐开发调试）
- 打开 Chrome：`chrome://extensions/`
- 开启“开发者模式”
- 选择“加载已解压的扩展程序”
- 选择目录：`D:\Codex_Projects\Zoucha_Shijue`
- 打开任意网页，点击扩展图标即可启动/关闭工具

2. 直接使用已有压缩包（分发）
- 仓库内已有：`visual-qa-extension.zip`
- 若你后续修改了源码，建议重新打包（见下节），确保 zip 与源码一致。

## 修改后重新打包（Windows）
在项目根目录执行：

```powershell
Compress-Archive -Path manifest.json,service_worker.js,visual-qa.js,icons -DestinationPath visual-qa-extension.zip -Force
```

## Store 素材与脚本
- 上架文案：`store-listing/listing-zh.md`
- 隐私政策：`store-listing/privacy-policy.md`
- 宣传图：`store-listing/promo-small.png`、`store-listing/promo-marquee.png`
- 截图脚本：`scripts/capture_store_shots.js`

运行截图脚本（需 Node + Playwright）：

```powershell
node scripts/capture_store_shots.js
```

输出目录：`store-listing/screenshots`

## 现阶段已知边界
- 测量与样式读取基于浏览器计算样式，复杂布局（transform、特殊 stacking/context）下可能与视觉感知有差异。
- 截图脚本的 cookie 弹窗处理是常见按钮匹配，目标站点变化时可能需要补 selector。
- 当前无持久化存储；刷新页面后状态重置。

## 后续可选迭代
1. 增加“复制测量结果”按钮，便于直接贴到缺陷单。
2. 增加简单设置项（快捷键映射、颜色主题、默认收起状态）。
3. 补一份面向非开发同学的 `README.md`（动图 + 10 秒上手）。
