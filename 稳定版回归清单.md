# 稳定版回归清单

## 发布基础检查

- `manifest.json` 中 `name` 为 `PixelAudit 视觉走查助手`。
- `manifest.json` 中 `short_name` 为 `PixelAudit`。
- `manifest.json` 中 `version` 为 `1.1.5`。
- release 目录为 `dist/visual-qa-release/`。
- 上架压缩包名称为 `dist/PixelAudit-visual-qa-1.1.5.zip`。
- `visual-qa.js`、`service_worker.js`、`content-bridge.js` 可通过 `node --check`。

## 0. AI 配置列表

- 修改一个元素后，AI 修改入口红点与数量状态正确。
- 刷新页面后，AI 配置列表能从本地恢复。
- 重新打开插件后，AI 配置列表能从本地恢复。
- 恢复后不会自动把历史 `patch.to` 重放到页面元素上。
- 恢复后全局复制 Prompt 仍可用。
- 恢复后单卡片复制 Prompt 仍可用。
- 恢复后点击卡片，能定位时重新选中，不能定位时只给轻提示。
- 删除当前会话内卡片时，会恢复页面上仍存在的对应样式。
- 删除恢复后的历史卡片时，如果页面已是原始状态，只删除记录，不报错。
- 删除动作不会重新生成新的 ChangePatch。
- 清空 AI 配置列表后刷新页面，不会恢复旧数据。

## 1. 选择与面板

- 点击元素后能正确锁定 A，面板内容刷新正常。
- 切换不同元素时，旧元素的编辑状态不会残留到新元素上。
- 收起面板、展开面板后，当前选中元素与标题展示正常。
- 面板内输入框、下拉框、颜色值字段都能正常编辑并提交。
- 文本元素清空文本后，selected panel 不会消失，文本输入框仍保留。
- 文本元素清空文本后，当前元素仍保持选中高亮或可编辑状态。
- 在空文本框重新输入内容后，页面文本会立即恢复，并继续作用在原文本元素上。
- 清空文本后切走再切回原元素，只要原元素仍有可点击区域，就还能识别为文本可编辑元素。
- 普通 `div / card` 不会因为没有文本而被误识别成文本编辑元素。

## 2. 样式编辑

- 实色填充可以稳定修改和回退。
- `backgroundColor` 编辑后，导出/恢复草稿后内容一致。
- `modifiedProps / editedProps` 在切换模式、Esc、收起面板后会正确清空。
- 不再出现渐变编辑入口误导用户。
- 宽高、字体、颜色、背景、描边、投影在文本态回归后仍可正常编辑。

## 3. 记录链路

- 元素记录可以创建、保存、编辑备注、删除。
- 区域记录可以创建、保存、编辑备注、删除。
- 问题说明输入框可以直接粘贴图片，并在面板中显示缩略图和删除按钮。
- 问题说明输入框聚焦时，`Command/Ctrl + V` 可粘贴图片或文字，不触发 `V` 模式快捷键。
- 输入框外按普通 `V` 时，选择模式快捷键仍然正常。
- 每条记录最多粘贴 3 张图片；图片处理期间保存按钮不可用，完成后可正常保存。
- 保存记录后，记录抽屉卡片在文字说明下方显示对应的“设计参考”缩略图。
- 记录抽屉可以打开、关闭，列表与空状态展示正常。
- 记录弹窗/抽屉关闭后，不会遗留 hover 冻结或半打开状态。
- AI 修改列表、加入记录、记录抽屉不受文本态修复影响。
- 锁定 A/B 且存在有效间距时，测量结果工具条显示“记录”和关闭图标。
- 工具条按钮 hover 无描边；持续移动鼠标时 hover 不会闪断。
- 点击关闭图标后仅清空 A/B，测量模式仍保持开启。

## 4. 草稿恢复

- 刷新页面后草稿能正常恢复。
- 旧版本草稿不会污染新版本 UI。
- 版本不匹配时能安全回退到空草稿。
- 保存、恢复、切换页面后草稿状态一致。
- 带说明截图的记录在刷新恢复草稿后，文字与图片均保持完整。

## 5. HTML 导出

- 导出的 HTML 顶部左侧显示页面标题和 URL。
- 右侧信息卡同时显示导出时间与记录总数。
- 导出时间格式为 `YYYY-MM-DD HH:mm:ss`。
- 导出后 HTML 能正常打开，问题列表与图片可见。
- 记录时粘贴的说明截图在对应问题的“设计参考截图”区域展示，点击可查看大图。
- 每条问题记录都有可点击和可键盘聚焦的“已完成”复选框。
- 勾选后卡片保持白底、边框变绿，“已完成”按钮为绿色实心，截图与详情同步弱化。
- 每条问题记录都有独立的“问题备注”输入框，输入不会修改原有“问题说明”。
- 刷新同一份报告后，问题备注和完成状态能从当前浏览器恢复；两者均不回写插件草稿。
- 点击“下载反馈版 HTML”后，新文件保留全部问题备注和完成状态，并能继续编辑和再次下载。
- 在无法使用浏览器本地存储时，报告提示通过“下载反馈版 HTML”保存当前内容。
- 在问题备注输入框粘贴截图后，对应问题下显示缩略图；点击可查看大图，删除操作可用。
- 每条问题最多保留 3 张反馈截图，超过限制时有明确提示。
- 下载反馈版 HTML 后，重新打开文件仍能看到备注截图，且不依赖外部图片文件。

## 6. Hover 闪烁回归

- 在选择模式和面板编辑过程中，hover 不会频繁闪烁。
- 面板内交互时 hover 冻结状态会按预期释放。
- Esc、切换模式、关闭菜单、收起面板后，hover 状态回到稳定态。
- `measureA / measureB` 在文本态回归后没有异常联动。
