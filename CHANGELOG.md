# Changelog

## 1.1.5 - 2026-09-04

### 修复
- 修复新增记录面板打开时，插件的 `V` 模式快捷键优先拦截 `Command/Ctrl + V`，导致问题说明输入框无法通过键盘粘贴图片的问题。
- 输入框获得焦点时，普通字母快捷键不再切换插件模式；复制、粘贴、剪切和全选等系统组合键保持原生行为。
- 修复记录保存后，记录抽屉只显示文字说明、没有显示已粘贴设计参考截图的问题。

### 发布
- 发布目录同步至 `1.1.5`，上架包为 `dist/PixelAudit-visual-qa-1.1.5.zip`。

## 1.1.4 - 2026-09-04

### 新增
- 插件“新增记录”的问题说明输入框支持直接粘贴设计稿或补充截图。
- 粘贴图片会在记录面板中显示缩略图，可在保存前删除；每条问题最多 3 张。
- 说明截图会随问题记录写入草稿，并在导出的 HTML 中作为“设计参考截图”展示和查看大图。

### 改进
- 保存按钮会在图片压缩期间暂时禁用，避免截图尚未处理完成就保存记录。
- 说明截图按体积逐级压缩，并启用扩展大容量本地存储，兼顾清晰度与草稿稳定性。

### 发布
- 发布目录同步至 `1.1.4`，上架包为 `dist/PixelAudit-visual-qa-1.1.4.zip`。

## 1.1.3 - 2026-09-04

### 新增
- 导出 HTML 的“问题备注”输入框支持直接粘贴截图，每条问题最多添加 3 张。
- 粘贴图片提供缩略图预览、点击查看大图和删除操作。
- 反馈截图会自动压缩并随“反馈版 HTML”一起保存，接收方无需另外获取图片文件。

### 改进
- 反馈截图优先保存到当前浏览器；本地存储不可用时，仍可通过下载反馈版 HTML 保留图片。
- 图片按体积逐级压缩，兼顾设计稿文字清晰度和报告大小。

### 发布
- 发布目录同步至 `1.1.3`，上架包为 `dist/PixelAudit-visual-qa-1.1.3.zip`。

## 1.1.2 - 2026-09-04

### 新增
- 导出的 HTML 报告为每条问题增加独立的“问题备注”输入框，方便开发直接填写处理情况、待确认事项或回复。
- 问题备注和“已完成”状态会自动保存在当前浏览器，刷新报告后可以继续编辑。
- 报告增加“下载反馈版 HTML”操作，可将已填写的备注和完成状态写入新文件并回传。

### 改进
- 原有只读“备注”更名为“问题说明”，与开发填写的反馈内容明确区分。

### 发布
- 发布目录同步至 `1.1.2`，上架包为 `dist/PixelAudit-visual-qa-1.1.2.zip`。

## 1.1.1 - 2026-09-04

### 修复
- 修复 HTML 报告在高清导出图缺失时误用 320px 缩略图并放大显示，导致截图文字模糊的问题。
- 新记录在保存前生成并持久化高清导出图，避免刷新页面或恢复草稿后丢失高清版本。
- 历史记录仅有缩略图时不再冒充高清图，报告会保持原尺寸显示并提示重新截图。

### 改进
- 截图质量提升至 JPEG 90，导出图调整为 1024px WebP 86，并启用高质量缩放，在控制文件大小的同时改善文字和细线清晰度。
- HTML 报告截图区域扩大至 720px，并优化窄屏响应式布局。
- 截图支持弹窗预览，默认适应窗口，可切换 100%、在 50%～200% 间缩放、双击切换视图，并支持滚动查看细节。
- 元素问题增加研发定位信息及复制按钮，便于开发人员快速查找对应页面元素。

### 发布
- 发布目录同步至 `1.1.1`，上架包为 `dist/PixelAudit-visual-qa-1.1.1.zip`。

## 1.1.0 - 2026-08-06

### 新增
- 导出的 HTML 报告为每条问题记录增加“已完成”勾选按钮，方便研发逐条确认修改进度。
- 完成态使用绿色实心按钮和绿色卡片边框，并弱化截图与详情，便于区分已完成和未完成条目。

### 发布
- 发布目录同步至 `1.1.0`，上架包为 `dist/PixelAudit-visual-qa-1.1.0.zip`。

## 1.0.9 - 2026-07-10

### 改进
- 双元素测量锁定后，结果工具条的关闭操作改为 28px 图标按钮；记录与关闭按钮使用无描边的悬停、按下和键盘焦点反馈。
- 工具条只在测量摘要变化时更新内容，鼠标移动过程中不再重建按钮节点，悬停反馈保持稳定。

### 发布
- 发布目录已同步至 `1.0.9`，生成 `dist/PixelAudit-visual-qa-1.0.9.zip` 作为上架包。

## 1.0.6 - 2026-05-12

### 修复
- 修复文本元素在 selected panel 中清空文本后，会立刻掉出文本编辑态的问题。
- 现在空文本状态下仍会保留当前元素选中、高亮和文本输入框，重新输入内容时继续作用在原文本元素上。
- 补充文本编辑回归约束，避免普通空 `div / card` 被误识别成文本元素。

## 1.0.5 - 2026-05-09

这是 `PixelAudit 视觉走查助手` 的阶段性稳定版收口发布。

### 本次收口范围
- 不新增功能，不重构核心逻辑。
- 统一 `manifest.json` 版本号到 `1.0.5`。
- 复核扩展名称、打包目录和发布产物命名。
- 补充当前版本功能说明与简版测试清单。

### 当前稳定能力
- 顶部工具栏与选择态编辑浮窗可正常使用。
- 元素/区域记录、记录抽屉、草稿恢复与 HTML 导出链路保留。
- AI 修改列表保留当前已完成能力，不继续向外扩展。

### 发布说明
- `manifest.json` 作为扩展版本唯一版本源。
- 本版本目标是阶段性完成，不再继续扩展当前能力边界。

## Archived Unreleased - 2026-04-07

### 交接摘要
- 已把顶部“测量”模式接管成 `select` 下的独立子态，避免打开右侧编辑框。
- 单元素 `single` 态已做稳，并收紧为“leaf-like / container-like”两类，只在元素自身明显是结构容器时才展示同组结构。
- 已移除 single 态里的绿色父容器框，0 值和 1px 噪声标签也已过滤。
- 双元素 `hover preview` / `pinned pair` 仍未开始，当前代码只停在 Step 2。

### 当前代码状态
- 非测量模式行为保持不变。
- 选择模式现有 `selected A + hover target` 主链路未改。
- `renderSelectedPanel`、`resolvePrimaryMeasure`、`addPrimaryMeasureGuides` 未改。
- 记录、抽屉、导出、草稿恢复未改。

### 已完成范围
- Step 1: 顶部测量入口接管、模式边界收口、`Esc` 清理测量态。
- Step 2: 单元素结构测量的基础实现和收口，包含 leaf-like / container-like 区分、padding/content 表达、gap/首尾空间展示和 fallback。

### 下一步
- 继续前先确认 Step 2 的视觉与结构判断是否符合预期。
- 若确认，再进入 Step 3 的双元素交互和渲染心智改造。

## 1.0.0 - 2026-03-27

这是 `视觉走查助手` 的首个正式发布基线。

### 核心能力
- 元素高亮与实时信息面板
- 冻结模式与间距引导
- 双元素测距
- 冻结态样式临时编辑与一键清除
- 面板固定、拖拽、收起/展开
- 快捷键：`M`、`F`、`C`、`R`、`P`、`B`、`Esc`

### 已知边界
- 默认按单页主框架工作，不额外扩展 iframe 兼容
- 测量与样式读取基于浏览器计算结果，复杂布局下可能与视觉感知存在差异
- 当前无持久化存储，刷新页面后状态会重置

### 发布说明
- `manifest.json` 以 `1.0.0` 作为唯一版本源
- 本版本保持现有功能边界，不再把当前修复继续向外扩展为新能力
