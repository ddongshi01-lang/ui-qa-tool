# 稳定版基线说明

## 当前基线

- 当前版本作为 `V1.0.6` 阶段性稳定版收口。
- 扩展名称保持为 `PixelAudit 视觉走查助手`。
- `manifest.json` 版本号统一为 `1.0.6`。
- 稳定版打包目录为 `dist/visual-qa-release/`。
- 稳定版压缩包名称为 `dist/PixelAudit-visual-qa-v1.0.6-stable.zip`。

## 当前保留的主文件

- `visual-qa.js`
- `service_worker.js`
- `content-bridge.js`
- `manifest.json`

## 当前版本功能说明

- 顶部工具栏：支持选择、测量、记录元素、记录区域等当前已接入入口。
- 选择态浮窗：支持查看当前选中元素信息，并在已开放字段内进行临时样式编辑。
- 测量能力：保留当前已稳定的间距、尺寸和元素间测量展示。
- 记录能力：支持元素记录、区域记录、记录列表查看、备注维护与删除。
- 草稿恢复：页面刷新或重新打开后，可恢复当前页面的记录与相关草稿状态。
- HTML 导出：支持将当前记录导出为可打开的 HTML 报告。
- AI 修改列表：保留当前已完成的修改记录、恢复、复制与删除能力，不继续扩展为新系统。

## 建议重点关注的主函数

- `buildHtmlExportDocument`
- `exportCurrentDraftAsHtml`
- `initializeV12DraftState`
- `loadDraftFromBridge`
- `persistCurrentDraft`
- `setV12Mode`
- `clearMeasureSelection`
- `setSelectedEl`
- `toggleV12Drawer`
- `handleEscapeKey`
- `createPendingRecord`
- `runPendingRecordShotCapture`

## 明确不建议继续大动的区域

- 记录截图与裁剪链路
- 草稿保存与恢复链路
- HTML 导出结构与样式
- 选择模式主流程
- 面板编辑主流程
- 记录抽屉主流程
- 顶部栏布局与快捷键映射

## 收口结论

- 现阶段只保留稳定的选择、面板编辑、记录、抽屉、草稿恢复、HTML 导出和当前已开放样式编辑路径。
- 不新增功能，不重构核心逻辑。
- 渐变、真实描边、真实投影和新的独立模式不再作为当前稳定版继续扩展目标。
