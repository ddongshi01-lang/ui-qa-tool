# 稳定版基线说明

## 当前基线

- 建议将当前版本作为 `stable-baseline` 收口基线。
- 版本号建议定为 `v1.2.1`。
- 如果要打 tag，推荐 `v1.2.1-stable`。

## 建议保留的主文件

- `visual-qa.js`
- `service_worker.js`

## 建议重点关注的主函数

- `buildHtmlExportDocument`
- `exportCurrentDraftAsHtml`
- `initializeV12DraftState`
- `loadDraftFromBridge`
- `persistCurrentDraft`
- `setV12Mode`
- `clearMeasureSelection`
- `setSelectedEl`
- `toggleV12Drawer`
- `handleEscapeKey`
- `createPendingRecord`
- `runPendingRecordShotCapture`

## 明确不建议继续大动的区域

- 记录截图与裁剪链路
- 草稿保存与恢复链路
- HTML 导出结构与样式
- 选择模式主流程
- 面板编辑主流程
- 记录抽屉主流程
- 顶部栏布局与快捷键映射

## 收口结论

- 现阶段只保留稳定的选择、面板编辑、记录、抽屉、草稿恢复、HTML 导出和实色填充路径。
- 渐变、真实描边、真实投影和独立测量模式不再作为当前稳定版的继续扩展目标。

